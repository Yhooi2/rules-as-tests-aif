# Prior-art evaluations — SSOT

> **Purpose:** single source of truth for build-vs-reuse decisions on this framework. Every capability area where we build something new (script, abstraction, dependency, lint rule, gate, recipe pattern) is evaluated against existing prior art before commitment. This file records that evaluation: what analog exists, when it was last reviewed, what verdict we reached, and the trigger condition that would re-open the decision.
>
> **Status:** shipped reference doc (≤500 LOC).
> **Created:** Phase 8.8 T1 (2026-05-08).
> **Consult gate:** [EXECUTION-PLAN.md §5.5 Step 1.5](EXECUTION-PLAN.md) — mandatory pre-build review (added Phase 8.8 T6).
> **Process discipline:** [CONTRIBUTING.md](../../CONTRIBUTING.md) + `Prior-art:` commit trailer convention (added Phase 8.8 T7).
> **Principle:** [`packages/core/principles/08-prior-art-cited.test.ts`](../../packages/core/principles/08-prior-art-cited.test.ts) validates that capability claims in research files cite entries in this file by ID (added Phase 8.8 T3).
>
> **Authoritative for:** build-vs-reuse SSOT register (entry table §4) + entry schema (§1) + verdict semantics (§2) + entry-add procedure (§3) + staleness policy (§5).
> **NOT authoritative for:** project goal — see [README.md#why-this-exists](../../README.md#why-this-exists). Recursive enforcement of Prior-art trailer in commits — see [.husky/pre-push](../../.husky/pre-push) hook + [CLAUDE.md](../../CLAUDE.md) trailer-syntax section.

---

## 1. Format specification

Each entry is a row in the table at §4 below. The row schema:

| Field | Type | Required | Description |
|---|---|---|---|
| `ID` | integer (monotonic) | ✓ | Stable identifier. Cite as `prior-art-evaluations.md#<ID>` from research files, retros, commit trailers. Once assigned, never reused. Increments only — gaps allowed if an entry is later removed (preserves citation stability). |
| `Candidate` | string | ✓ | Name of the prior art (project, paper, vocabulary, framework, blog post). Include version / commit / publication date if relevant. |
| `Capability matched` | string | ✓ | Which capability area of *this* framework the candidate maps to. Examples: «L3 LLM-driven rule generation», «L1 multi-framework version-aware detection», «framework-level meta-test pattern vocabulary». Be specific — vague matches like «testing» get rejected at review. |
| `First seen` | ISO 8601 date | ✓ | When the candidate first surfaced in our research, regardless of verdict. Frozen at entry creation. |
| `Last reviewed` | ISO 8601 date | ✓ | When the entry was last actively re-examined for current applicability. Updated whenever §5.5 Step 1.5 consult matches this entry, regardless of whether the verdict changes. |
| `Verdict` | enum | ✓ | One of `ADOPT` / `DEFER` / `WATCHLIST` / `REJECT`. See §2 for semantics. May be qualified (e.g. `ADOPT VOCABULARY` means adopt the framing but not the code). |
| `Rationale` | string (≤500 chars) | ✓ | Why this verdict. Must reference concrete differences (not «didn't fit») and cite at least one external link, prior-art entry, or open-questions ref. |
| `Trigger to revisit` | string | ✓ | The precondition under which this entry's verdict should be re-opened. Examples: «LLM v2 trigger fires per [open-questions.md §13.10 entry #1]», «Phase 9+ detector v2 entry research», «new framework requires version-aware detection». An entry without a non-trivial trigger condition is a sign of weak rationale. |

---

## 2. Verdict semantics

| Verdict | Meaning | Action implied |
|---|---|---|
| `ADOPT` | Use the candidate directly — adopt as a dependency, ship its API surface, or reuse its artifacts. | Add as explicit dep / vendored module / cited algorithm; document in [EXECUTION-PLAN.md §3.3 Prior art table](EXECUTION-PLAN.md). |
| `ADOPT VOCABULARY` | Sub-form of `ADOPT`. Adopt the candidate's *framing or terminology* without adopting code. Useful when an established name exists for what we built. | Update doc / README / glossary to use the established term; cite the candidate. |
| `DEFER` | Candidate solves a related problem in a different domain, or has incompatible constraints. Not used now, but the option stays open. | Document the difference in `Rationale`; specify `Trigger to revisit`. Do **not** silently rebuild the candidate's surface — re-examine when trigger fires. |
| `WATCHLIST` | Candidate is mature in production but blocked by our current stop-rules / scope (e.g. deterministic-v1 stance forbids new deps). Re-examine when stance shifts. | Document the blocking stop-rule; trigger condition references the stop-rule's relax-criterion. |
| `REJECT` | Candidate is **not** applicable — fundamentally different problem space, abandoned project, security disqualification, license incompatible. Re-opening requires new evidence (not just time). | Document concrete disqualifier; trigger condition must specify what would invalidate the disqualifier (rare). |

**Heuristic:** if the rationale would be the same one year from now without any new evidence, the verdict is `REJECT`. Otherwise it's `DEFER` / `WATCHLIST`.

---

## 3. How to add an entry

1. **§5.5 Step 1.5** (phase research): when listing capability areas, run `context7` queries (≥3 phrasings) for each capability. For each candidate that surfaces:
   - Check this file for an existing entry on the candidate. If present: update `Last reviewed` and re-evaluate per its `Trigger to revisit`.
   - If absent: add a new row to §4 with the next `ID`.
2. **Capability commits** (per [CONTRIBUTING.md](../../CONTRIBUTING.md) — added T7): include a `Prior-art:` commit trailer citing the relevant entry by ID, even when the verdict was set in an earlier commit.
3. **Edits to existing rows** are tracked via git history — no in-file changelog. The file's git log is the audit trail.

### Entry template (paste into §4 table)

```text
| <next ID> | <Candidate name + version/date> | <Capability area, specific> | <YYYY-MM-DD first seen> | <YYYY-MM-DD reviewed today> | <ADOPT|DEFER|WATCHLIST|REJECT> | <Rationale ≤500 chars with at least one cited link/ref> | <Trigger condition> |
```

---

## 4. Entry table

| ID | Candidate | Capability matched | First seen | Last reviewed | Verdict | Rationale | Trigger to revisit |
|---:|---|---|---|---|---|---|---|

<!-- Entries are appended below this line. Do not edit existing rows in place except for `Last reviewed` updates per §3 step 1; substantive verdict changes are new commits with git history audit. -->
| 1 | Autogrep (Semgrep + LLM, 2024–2025) | L3 LLM-driven rule generation | 2026-05-08 | 2026-05-08 | DEFER | Closest single analog per ultraview verdict 2026-05-08; context7 lookup 2026-05-08 confirms Semgrep auto-generates rules from CVE / GHSA / Electron release-note feeds (security-only domain). No stack-aware `stack:[]` field, no self-application invariant, no best-practice-doc source signal — fundamentally different problem space (CVE patches → rules vs. framework version-bump docs → rules). Re-evaluate when LLM v2 trigger fires per [open-questions.md §13.10 entry #1](open-questions.md). **Phase 9 entry consult (2026-05-08):** trigger condition fired — [open-questions.md §13.10 entry #2](open-questions.md) (Path A LLM gen) fired at Phase 8 close per [retros/phase-8.md Open Q #1](retros/phase-8.md); re-evaluation performed via fresh context7 lookup in T3, verdict review in [phase-9-entry-research.md §5](phase-9-entry-research.md). DEFER stance carries forward unless T3 surfaces new Semgrep rule-synthesis-from-docs feature post-2026-05-08. | LLM v2 trigger fires; OR Phase 9+ entry research surfaces new Autogrep release / Semgrep rule-synthesis-from-docs feature |
| 2 | Netlify CLI `framework-info` / `detect-server-settings` (production CLI plugin) | L1 multi-framework version-aware detection | 2026-05-08 | 2026-05-08 | WATCHLIST | Production-ready multi-framework detection — context7 lookup 2026-05-08 confirms `netlify dev --framework` flag with `#auto` default, plus `src/utils/detect-server-settings.ts` orchestrating framework detection across Next.js / Fastify / Hugo / others. Heuristics include Next 13.5+ App Router probe and Fastify entrypoint sniffing. **Blocked now by deterministic-v1 stop-rule (§6.0 #2 — no new explicit deps).** Vendoring detection logic violates the spirit of the rule even with transitive-only deps. WATCHLIST verdict means: when stop-rule relaxes (Phase 9+ stance shift) OR a new framework requires version-aware detection that our hand-rolled detector can't cover, re-open. | Phase 9+ detector v2 entry research; OR new framework target requires version-aware semantics our v1 detector can't express; OR §6.0 #2 stop-rule relaxes |
| 3 | Fitness functions (Ford / Parsons / Kua, *Building Evolutionary Architectures*, 2017 / 2nd ed. 2023) | framework-level meta-test pattern vocabulary | 2026-05-08 | 2026-05-08 | ADOPT VOCABULARY | Established academic/industry term for exactly the principles-as-tests pattern shipped in Phase 2 (`packages/core/principles/01-07*.test.ts`, now +08 from T3). The term «fitness function» captures «automated invariant test for an architectural property», which is precisely what 01-08 are. Vocabulary adoption only — no code change, no dep, no behavioral change. Adopting the framing improves onboarding (readers familiar with evolutionary architecture map to our Phase 2 immediately) and academic citation. Implementation: T10 updates [overview.md L2](../skills/rules-as-tests/references/overview.md) terminology where applicable. (Vocabulary entry — context7 not consulted; the term is documented in published books not indexed as a library, and re-verification is unnecessary for a stable academic reference.) | New competing vocabulary supersedes «fitness functions» in industry/academic discourse; OR scope of principles widens beyond what the original term covers |
| 4 | Factory ESLint Plugin (`@factory/eslint-plugin`, open-source 2025-2026 fork of Factory's internal tooling) | framework-aware curated AI-generated-code rule plugin (potential menu reference for Path A LLM gen) | 2026-05-08 | 2026-05-08 | WATCHLIST | Production-grade ESLint plugin, 21 hand-authored rules across `base`/`recommended`/`frontend`/`backend` configs, explicitly «designed to improve AI coding agent output quality by enforcing custom lint rules» (per `/factory-ai/eslint-plugin` context7 lookup 2026-05-08). Includes Next.js-aware rules (e.g. middleware-on-route-handlers); comprehensive per-rule Markdown for AI agent adaptation; explicit reuse pattern «projects publish own custom plugin using Factory as template». **Not LLM-driven «picks from menu»** — rules are hand-authored — but the closest production analog to a curated menu Path A LLM gen would select from. Vendoring violates [§6.0 #2](EXECUTION-PLAN.md) (no new explicit deps); referencing as exemplar is the v1-fit path. Re-evaluate when Phase 9+ Path A LLM gen activates AND our recipe inventory exceeds hand-roll capacity. | Phase 9+ Path A LLM gen activates AND curated recipe count exceeds hand-roll capacity; OR new AI-code-quality framework target requires plugin-style menu we don't yet ship; OR [§6.0 #2](EXECUTION-PLAN.md) stop-rule relaxes |
| 5 | Anthropic `web_search_20250305` server tool (TypeScript SDK, production 2025-2026) | LLM-driven live web research with allowlist/blocklist domain controls (L2 Research Agent v2 trigger surface) | 2026-05-08 | 2026-05-08 | ADOPT WHEN TRIGGERED | Per `/anthropics/anthropic-sdk-typescript` context7 lookup 2026-05-08: first-class server tool with `allowed_domains` / `blocked_domains` (mutually exclusive), `max_uses` budget cap, `cache_control` for caching, `user_location`, `defer_loading`, stream-compatible. Same spec name (`web_search_20250305`) referenced in [open-questions.md §13.10 entry #1](open-questions.md) as the canonical L2 LLM research extension surface. Phase 9 entry confirms the tool is current and SDK-supported; no third-party SDK with equivalent first-party allowlist surface in context7 search. Trigger «first real consumer reports gap on non-curated framework, OR Phase 8 acceptance shows curated store insufficient» has NOT fired — Phase 8 closed without curated-store gap per [retros/phase-8.md Open Q #2](retros/phase-8.md). When trigger fires, this is the canonical implementation. | [open-questions.md §13.10 entry #1](open-questions.md) trigger fires (consumer gap on non-curated framework, OR curated store insufficient for Next 16+ patterns) |
| 6 | Arc42 §1/§2/§6 (goal/methodology/invariant split) | Doc structure for goal hierarchy | 2026-05-09 | 2026-05-09 | ADOPT VOCABULARY | Adopted in `README.md§Why this exists` and rule `.claude/rules/doc-authority-hierarchy.md` per goal-hierarchy research 2026-05-09 §1 — only production framework with explicit goal/methodology/invariant separation. **Velocity: SLOW (~2yr major cadence; v9.0 EN July 2025).** | 365-day staleness; OR Arc42 v10 ships changing §1/§2/§6 semantics |
| 7 | AGENTS.md spec (Linux Foundation AAIF, Dec 2025) | AI-doc convention; «README owns goal» | 2026-05-09 | 2026-05-09 | ADOPT VOCABULARY | Donated by OpenAI to LF AAIF Dec 2025; adopted by 60k+ projects. Used as authority signal in `CLAUDE.md` goal pointer. **Velocity: UNCERTAIN — v2 risk concentrated next 12 months given recent stewardship transition.** | 90-day check until 2026-12; promote to 180-day if no v2 by then; OR LF AAIF announces breaking change |
| 8 | AIF Step 0 re-read pattern (`/aif-*` skill bootstrap) | Compaction-resilient session anchoring | 2026-05-09 | 2026-05-09 | ADOPT VOCABULARY | Adopted in `.claude/session-bootstrap.md` — code-enforced re-read of stable on-disk file at session start. More robust than CLAUDE.md compaction-block (Anthropic does not dogfood the latter in `claude-code` repo). **Velocity: FAST (AIF active 2.x; 1.x→2.x had breaking changes).** | 90-day context7 check on `/lee-to/ai-factory`; OR AIF major version bump |
| 9 | Cline Memory Bank subset (Mermaid diagrams in instructions) | Drift-prevention via state-machine diagrams | 2026-05-09 | 2026-05-09 | ADOPT VOCABULARY | Adopted in `.claude/session-bootstrap.md` reviewer-cycle drift-prevention diagram. Full 6-file Memory Bank pattern deferred per [open-questions.md §13.19](open-questions.md). **Velocity: FAST (Cline framing: «methodology, not a fixed feature»).** | 90-day check on Cline docs; OR community PRs change format substantially |
| 10 | matklad ARCHITECTURE.md authority-scope pattern | Per-doc Authoritative-for header convention | 2026-05-09 | 2026-05-09 | ADOPT VOCABULARY | Foundational source for doc-authority-hierarchy rule §3 format. Adopted across 30+ project-internal docs in this branch. **Velocity: STABLE (single 2021 blog post, no updates expected; pattern is well-known but unnamed industry-wide).** | Accept as-is — re-verify only if matklad publishes follow-up post explicitly revising the pattern |
| 11 | ESLint shareable config `extends:` (eslint.org docs, v9 flat config, 2026-05) | Three-layer authority composition: framework default + consumer override + escape hatch | 2026-05-09 | 2026-05-09 | ADOPT VOCABULARY | Canonical "user wins last" pattern; cited in [§13.21 L3 revision §3 Layer 3](research-patches/2026-05-09-§13.21-l3-revision.md). Vocabulary adoption only — no code dep, no behavioral change. Documents framework's authority model in industry-recognised terms. **Velocity: STABLE (ESLint 9.x flat config; major version every 18-24 mo).** | Major ESLint config-format change post-flat-config; OR new shareable-config pattern emerges as canonical |
| 12 | Tailwind CSS `presets` array composition (tailwindcss.com docs, v4 2026) | Composable framework defaults with project extension | 2026-05-09 | 2026-05-09 | ADOPT VOCABULARY | Identical "project extends preset, later overrides earlier" semantics; cited in [§13.21 L3 revision §3 Layer 3](research-patches/2026-05-09-§13.21-l3-revision.md). Vocabulary adoption — same justification as #11. **Velocity: SLOW (Tailwind v4 stable; presets API unchanged since v3).** | Tailwind v5 changes preset semantics; OR project deprecates `presets` field |
| 13 | Cursor Team / Project / User Rules 4-tier precedence (cursor.com docs, 2026) | Multi-tier authority precedence (deliberately authoritarian) | 2026-05-09 | 2026-05-09 | DEFER | Inverted from initial recommendation: Cursor Team-wins-highest contradicts AGENTS.md README-owns-goal convention adopted by framework. Documented as alternative-considered in [§13.21 L3 revision](research-patches/2026-05-09-§13.21-l3-revision.md); NOT adopted. **Velocity: FAST (Cursor ships rule-system changes quarterly).** | Framework introduces org-level Team Rules surface; OR consumer requests authoritarian-default mode |
| 14 | Continue.dev Hub Rules `uses:` runtime reference (continue.dev docs, 2026) | Runtime-resolved AI-doc reference (alt. to static-copy) | 2026-05-09 | 2026-05-09 | DEFER | Cited in [§13.21 L3 revision §4 update-mechanism matrix](research-patches/2026-05-09-§13.21-l3-revision.md); rejected for current framework due to network dep, no audit trail. DEFER not REJECT — re-opens if framework gains hosted-config surface. **Velocity: FAST (Hub spec evolving; weekly schema changes possible).** | Framework ships hosted-config surface (Phase 11+); OR consumer requests centralised-rules mode |
| 15 | AGENTS.override.md escape-hatch (Codex / OpenAI 2026-Jan spec) | Wholesale-replacement primitive for cumulative-inheritance docs | 2026-05-09 | 2026-05-09 | ADOPT VOCABULARY | Adopted as consumer-side override mechanism per [§13.21 L3 revision Wave 4 deliverable](research-patches/2026-05-09-§13.21-l3-revision.md). Vocabulary adoption documents pattern's name in INSTALL-FOR-AI.md (Wave 4); no shipping-code change beyond docs. **Velocity: UNCERTAIN — mirrors AGENTS.md spec entry #7 (LF AAIF stewardship transition).** | 90-day check until 2026-12 (mirrors entry #7); OR LF AAIF announces breaking change to override semantics |
| 16 | Goal-phrase parity check between primary source doc and downstream docs (context7 sweep 2026-05-11) | D-3 probe: detecting drift when the canonical goal phrase is paraphrased or omitted in downstream goal-bearing docs | 2026-05-11 | 2026-05-11 | BUILD | **Context7 sweep (3 phrasings, 2026-05-11):** (1) «doc-vs-doc parity / drift detection» → Drift SDK (DeFi), anomaly detection resources — no doc-parity tool; (2) «AI documentation paraphrase drift template rendered output» → Documentation.AI (documentation platform, not a parity checker), Mistral/Google Document AI (OCR/extraction) — different capability; (3) «documentation template consistency downstream parity» → Doc Detective (tests docs against product behaviour, not doc-vs-doc phrase parity), NgDoc (Angular docs generator) — different domain. No production-grade analog for goal-phrase presence check between a primary source doc and enumerated downstream docs. BUILD verdict — hand-rolled probe in `packages/core/audit-self/audit-ai-docs.sh` D3 (sub-wave 7.1.d). Shipped: bash probe + synonym list documented inline. | Dedicated doc-parity tool surfaces in context7 or npm with production adoption; OR Wave 7 template test (7.3.d) P1 probe generalises to cover this case; re-evaluate at 7.5.b. |
| 17 | markdownlint-cli2 (DavidAnson, v0.13+ stable 2025-2026, CLI fork of markdownlint-cli with config-resolver + workspace globs) | Structural markdown lint (heading levels, list formatting, blank lines, trailing whitespace) at pre-commit; pre-commit hook + CI gate | 2026-05-10 | 2026-05-11 | ADOPT | Context7 sweep (Wave 7 research patch §1 + 3 phrasings 2026-05-10): production-grade structural markdown linter, MIT-licensed, zero runtime deps beyond Node. No equivalent JS-native competitor with config-resolver + workspace globs (Vale targets prose, not structure; remark-lint requires unified plugin chain — heavier setup). Shipped in `80ef1d9` (sub-wave 7.1.a) with hand-rolled ≤30-line `.markdownlint.json` per Decision 6. **Velocity: STABLE (v0.13 line; competing structural linters absent).** | Major version bump 0.x→1.x with breaking config-schema change; OR new JS-native structural markdown linter emerges as canonical; OR Vale gains structural-linting mode (currently prose-only) |
| 18 | Vale (errata-ai/vale, prose linter, v3.x stable 2026) | Prose-register and vocabulary lint for documentation markdown (complement to markdownlint-cli2 structural lint) | 2026-05-10 | 2026-05-11 | DEFER | Context7 sweep (Wave 7 research patch §1 phrasing 5, 2026-05-10): production-grade prose linter with per-style rule packages (Microsoft, Google, Vale, Readability). DEFER per Decision 2 (2026-05-11): project mixes Russian + English prose; English-style preset FP rate ≥30% on Russian corpus per Wave 7 §6 adversarial check ([research-patches/2026-05-10-wave-7-review-verdicts.md](research-patches/2026-05-10-wave-7-review-verdicts.md) §Decision 2). markdownlint-cli2 (SSOT #17) closes structural-drift class for which sample-size>0 exists; Vale targets orthogonal prose-quality layer with sample-size=0 incidents currently. **Velocity: FAST (errata-ai ships rule packages quarterly; base CLI stable).** | 2nd documented doc-prose-drift incident that markdownlint-cli2 cannot catch; OR Vale ships a Russian/mixed-corpus profile with FP rate <10% on project corpus |
| 19 | lychee (/lycheeverse/lychee, Rust-based async link checker, v0.x stable 2026) | Async offline link integrity checking for markdown cross-references (pre-push, scoped to changed `*.md`) | 2026-05-10 | 2026-05-11 | ADOPT | Context7 sweep (Wave 7 research patch §1 phrasing 6, 2026-05-10): production-grade fast async link checker; supports MD + HTML + .txt; offline-first via `--offline`; scoped per-file via glob or staged-only. ADOPT verdict transition from ADOPT WHEN TRIGGERED (research patch): trigger fired — Wave 7 sub-wave 7.1.b shipped lychee in `.husky/pre-push` section 5 (commit `71a7f00`) as graceful-skip when absent (opt-in install per cargo or brew). No Node/Python dep needed. **Velocity: MEDIUM (active Rust project; v0.x pre-1.0; breaking changes possible on minor bumps).** | lychee ships breaking CLI surface change; OR JS-native async link checker emerges as canonical; OR install barrier surfaces as adoption blocker for >30% of contributors |
| 20 | Claude Code hooks API (Anthropic, `settings.json` hooks — UserPromptSubmit + PostToolUse, 2025-2026) | Harness-level write-time enforcement layer — 5th lifecycle stage (fires at tool-use time, before git checkpoint) | 2026-05-10 | 2026-05-11 | ADOPT | Context7 sweep (Wave 7 research.md §3 + §4, 2026-05-10): native `PostToolUse` / `PreToolUse` / `UserPromptSubmit` event-driven hooks; JSON decision contract; stdout-to-Claude feedback channel for non-blocking advisory. ADOPT verdict transition from ADOPT WHEN TRIGGERED: trigger fired — Wave 6 Probe 2 FAIL + §13.28 default-B path + Wave 6 D-1 DEFER→Wave 7. Shipped: `5c0d32e` (UserPromptSubmit session-bootstrap), `2e43874` (PostToolUse validate-prompt), `38dc50a` (PostToolUse principle-09). Cross-editor parity WATCHLIST per SSOT #21. **Velocity: FAST (Anthropic ships Claude Code changes quarterly; hook API surface actively evolving).** | 90-day check on Anthropic Claude Code docs; OR Anthropic announces breaking change to hook contract format |
| 21 | Cross-editor harness-hook parity: Cursor hooks (community, beautyfree/cursor-hook), Cline hooks (native), Codex CLI hooks, Continue.dev Hub `uses:` references (production, 2026) | Cross-editor harness-hook parity surfaces — parallel to Claude Code #20 for Cursor/Cline/Codex/Continue consumers | 2026-05-10 | 2026-05-11 | WATCHLIST | Context7 sweep (Wave 7 research.md §3, 2026-05-10): each editor ships its own PreToolUse/PostToolUse surface; no unifying framework. Cursor Hook is community-built (upstream risk); Cline hooks native but JSON-cancel-only; Continue.dev Hub `uses:` is network-dependent. Claude Code is primary — project's `.claude/hooks/` artefacts are Claude Code-specific per Decision 4 (2026-05-11). Cross-editor parity is consumer task, documented in INSTALL-FOR-AI.md. WATCHLIST: not adopted project-side; reference shape for consumer adaptation. **Velocity: FAST (all four editors ship changes quarterly or faster).** | Consumer audit shows Cursor/Cline/Codex adoption signal; OR ≥3 consumers on non-Claude-Code editors report hook coverage gap; OR cross-editor parity becomes a project invariant |
| 22 | Cookiecutter (audreyfeldroy, Python, v2.x stable 2025-2026) + Copier (copier-org, Python, v9.x stable 2025-2026) | Template rendering with answer-file replay; functional template-rendering test pattern («render template into tmp dir, assert against rendered output») | 2026-05-10 | 2026-05-11 | ADOPT VOCABULARY | Context7 sweep (Wave 7 research patch §2 + 3 phrasings 2026-05-10): canonical project-scaffolding-with-replay vocabulary. Both Python-based (vendoring violates §6.0 #2 no-new-dep stop-rule per entry #2 reasoning). Adopted as **vocabulary only** — pattern «render template into tmp dir, assert deterministic probes pass» named «template-render audit» in [packages/core/audit-self/template-render.audit.ts](../../packages/core/audit-self/template-render.audit.ts) (sub-wave 7.3.a/b, commit `f528586`), mirroring Copier's `answers.yml` replay analog. No Python runtime adopted. **Velocity: SLOW (Cookiecutter 2.x stable; Copier 9.x stable).** | Consumer requests Python-based template runtime (force vendoring); OR new JS-native template-engine emerges as canonical (e.g. shadcn-cli-style scaffolders evolve to general-purpose); OR Copier ships answer-file-replay primitive consumable from Node without Python interpreter |
| 23 | commitlint (`/conventional-changelog/commitlint`, High reputation, v19.x 2026) | Commit-message trailer enforcement via custom plugin rules | 2026-05-11 | 2026-05-11 | ADOPT VOCABULARY | Context7 sweep (Wave 7 sub-wave 7.6.a research §0 phrasings 1+5, 2026-05-11): custom plugin pattern `signed-off: ({ raw }) => /^Signed-off-by:/m.test(raw)` maps directly to a §1.7 trailer presence check. Semantic gap: commitlint fires at commit-time (`commit-msg` hook), while §13.23 targets push-time (`pre-push`, revision range `origin/main..HEAD`). We implement the same grep pattern in bash (`s17_check_trailer()` in `.husky/pre-push` section 9) without the dep. Vocabulary: «custom trailer enforcement via regex on raw commit body» is production-grade and well-documented. No dep adoption. **Velocity: STABLE (v19.x; major version every ~12-18 mo).** | commitlint ships a push-range enforcement surface (pre-push mode); OR project stack already includes commitlint dep from another consumer; OR §9 hand-roll grows beyond 2 substance arms (currently: Wave 8.3 file:line citation + Wave 9.4 body-prose detection) — at that point commitlint consolidation justifies dep adoption |
| 24 | semantic-release (`/semantic-release/semantic-release`, High reputation, v24.x 2026) | Commit footer/trailer parsing for release automation | 2026-05-11 | 2026-05-11 | DEFER | Context7 sweep (Wave 7 sub-wave 7.6.a research §0 phrasing 2, 2026-05-11): semantic-release reads commit trailers (`BREAKING CHANGE:`, `fix:`, `feat:`) to compute version bumps — release automation, not an enforcement gate. Does NOT block commits lacking trailers. Different problem space from §13.23 enforcement need. DEFER pending release-automation adoption. **Velocity: FAST (monthly release cadence).** | Project adopts automated versioning; OR semantic-release ships an enforcement-plugin mode that blocks pushes missing required trailers |
| 25 | commitizen Python CLI (`/commitizen-tools/commitizen`, Medium reputation, v3.x 2026) | Push-range commit validation (`cz check --rev-range`) | 2026-05-11 | 2026-05-11 | DEFER | Context7 sweep (Wave 7 sub-wave 7.6.a research §0 phrasing 3, 2026-05-11): `cz check --rev-range master..HEAD` is the closest behavioural analog to our pre-push range validation (push-range validation has established precedent — vocabulary signal). However: Python dep incompatible with Node-native stack; schema validation targets message FORMAT, not conditional trailer PRESENCE per diff content. No adoption. **Velocity: MEDIUM (active 2.x→3.x migration).** | Project adopts Python tooling; OR a Node-native push-range commit validator with similar API surfaces; OR commitizen ships a JS-native edition with equivalent push-range semantics |
| 26 | git interpret-trailers (git builtin, RFC 822 standard) | RFC822-style trailer parsing + order-independent key extraction | 2026-05-11 | 2026-05-11 | ADOPT VOCABULARY | Context7 sweep (Wave 7 sub-wave 7.6.a research §0 phrasing 4, 2026-05-11): `git interpret-trailers --parse` extracts key:value pairs order-independently per RFC 822. Confirmed the existing `pa_check_trailer()` in `.husky/pre-push` uses bash grep, NOT this builtin — adopting `git interpret-trailers` for the new §1.7 check while retaining grep for `Prior-art:` would create a two-parser inconsistency. `s17_check_trailer()` mirrors the existing bash `case`-match pattern; two-parser inconsistency avoided. Vocabulary adoption: RFC822 informs trailer format design (key: value, blank-line separation from body). No runtime adoption. **Velocity: STABLE (git core; breaking changes near-impossible).** | `pa_check_trailer()` is refactored to `git interpret-trailers` (F3 fallback per 7.6.b review §8); OR project decides to unify all trailer parsing in a future hook refactor |
| 27 | AIF Handoff `HANDOFF_MODE` env-var fork (lee-to/aif-handoff main, `docs/mcp-sync.md`) | Env-var driven mode fork for orchestrator: autonomous (no MCP calls + no interactive prompts) vs. interactive (inline `handoff_sync_status` MCP sync) | 2026-05-11 | 2026-05-16 | DEFER | Context7 sweep (aif-handoff overlap mandate 2026-05-11, 8 phrasings against `/lee-to/ai-factory`): `HANDOFF_MODE=1` suppresses interactivity and delegates status management to external Handoff coordinator — more automation-portable than our file-prompt Mode B (P4). Different problem class: AIF fork = external-system integration suppression; ours = model-quota + context-isolation. C1×P4 ORT per #pattern-matching-on-name analysis ([research-patches/2026-05-11-aif-handoff-overlap-analysis.md §3](research-patches/2026-05-11-aif-handoff-overlap-analysis.md)). DEFER pending first non-interactive orchestration pipeline. **Attribution corrected 2026-05-16:** `HANDOFF_MODE` env var lives in `lee-to/aif-handoff main, docs/mcp-sync.md` (confirmed via context7 query `/lee-to/aif-handoff` 2026-05-16 — mcp-sync.md explicitly defines Skills read `HANDOFF_MODE`, `HANDOFF_TASK_ID`, `HANDOFF_SKIP_REVIEW` env vars at load time). Original citation `lee-to/ai-factory v2.x, skills/aif-implement/SKILL.md` was wrong-repo (ai-factory 2.x referenced HANDOFF_TASK_ID as a bridge env var, but HANDOFF_MODE fork logic is in aif-handoff). **Velocity: FAST (AIF 2.x active).** | ≥2 reviewer-reported file-prompt session-loss incidents in one quarter logged under `#mode-b-fragility` in research-patches/; OR orchestrator session lifecycle docs explicitly describe Mode B failure mode; OR Wave 5 §13.25 implementation surfaces AIF Handoff MCPs as discoverable candidates |
| 28 | AIF Handoff `paused:true/false` semantic (lee-to/aif-handoff main, `@aif/shared` — `packages/shared/src/stateMachine.ts` + `packages/data/src/index.ts`) | Explicit machine-readable pause-for-human-review primitive in workflow state transitions (`"review"` status with `paused:true`) | 2026-05-11 | 2026-05-16 | DEFER | Context7 sweep (aif-handoff overlap mandate 2026-05-11): `paused:true` on `"review"` status encodes «human review required before AI continues» machine-readably. Our workflow has implicit pause points (P5 reviewer gate, orchestrator decision per `.claude/skills/reviewer/SKILL.md`) but no machine-readable pause state. S1×P5 ORT (different granularities: record-level MCP call vs. session-level discipline). Useful primitive for Phase 11+ wave orchestration once state machine formalised. **Attribution corrected 2026-05-16:** `paused` field defined in `@aif/shared` (type `Task`, `packages/shared/src/`); state machine in `packages/shared/src/stateMachine.ts`; paused-task exclusion in `packages/data/src/index.ts`. States: `backlog`, `planning`, `plan_ready`, `implementing`, `review`, `blocked_external`, `done`, `verified`. The `handoff_sync_status` MCP tool (in `@aif/mcp`, not `ai-factory`) can atomically set/clear `paused` with status change. Original citation `lee-to/ai-factory v2.x, handoff_sync_status tool` was wrong-repo (confirmed via DeepWiki ask_question 2026-05-16 — <https://deepwiki.com/search/where-is-the-pausedtrue-field_5b694046-a22b-45d7-a9ff-8d520cdb6ce1>). **Velocity: FAST (AIF 2.x active).** | ≥3 simultaneous waiting states in orchestrator-prompts/ requiring separate lookups; OR pipeline automation requires machine-readable state without human presence; OR Phase 11+ autonomous orchestration (full state machine). **Partial ADAPT adopted (2026-05-11):** `status:` frontmatter convention in wave kickoff files (doc-only, gitignored) enables grep-able state tracking at current scale; AIF full paused-semantic remains DEFER'd pending full state machine need. |
| 29 | AIF `<!-- handoff:task:<id> -->` first-line plan annotation pattern (PAIR: lee-to/ai-factory v2.x `HANDOFF_TASK_ID` env-var convention ↔ lee-to/aif-handoff main `@aif/shared` `insertPlanAnnotation`/`parsePlanAnnotations` + `@aif/agent` Planner subagent) | Machine-parseable artifact-to-scope linking via first-line HTML comment annotation in work files | 2026-05-11 | 2026-05-16 | ADAPT | Context7 sweep (aif-handoff overlap mandate 2026-05-11): `<!-- handoff:task:<HANDOFF_TASK_ID> -->` as mandatory first line of plan files links plans to Handoff tasks machine-parseably. Adaptation for research patches: `<!-- scope:§N -->` as first line enables automated §1.6 trigger sweeps replacing prose grep. Modifications: `§N` syntax (not UUID); scope to `docs/meta-factory/research-patches/` only; enforcement gate in `packages/core/principles/` or `audit-self.yml`. **Backward-sweep prerequisite (§1.7):** ~24 existing research patches require retrofitting annotation atomically with CI gate. AIF A1×P6 OVL(ours) for audit-trail; ADAPT wins on machine-parseability for trigger automation. **Attribution corrected 2026-05-16:** PAIR-attribution required — annotation convention originates in `lee-to/ai-factory v2.x` (Planner receives `HANDOFF_TASK_ID` env var and injects annotation as first line of plan); annotation insertion/parsing logic lives in `lee-to/aif-handoff main, @aif/shared` (`insertPlanAnnotation`/`parsePlanAnnotations` exported from `packages/shared/src/sync.ts`); Planner subagent in `@aif/agent` (`packages/agent/src/subagents/planner.ts`) injects it when `HANDOFF_TASK_ID` present. Original citation `lee-to/ai-factory v2.x, skills/aif-plan/SKILL.md` was incomplete (consumer side — `@aif/shared` + `@aif/agent` — was missing). Confirmed via DeepWiki ask_question 2026-05-16 — <https://deepwiki.com/search/is-there-a-handofftask-annotat_7de90b12-5d36-4f6a-952f-5145f2245d78>. **Velocity: FAST (AIF 2.x active).** | §1.6 trigger sweep volume >20 armed entries where grep-on-prose becomes brittle; OR Phase 11+ tooling requires machine-linked patch→trigger cross-reference; backward-sweep prerequisite: atomic with CI gate per §1.7 |
| 30 | AIF Handoff Planner/Implementer/Reviewer subagent pipeline (lee-to/aif-handoff main, `@aif/agent` — `packages/agent/src/subagents/planner.ts` + `implementer.ts` + `reviewer.ts`, orchestrated by `packages/agent/src/coordinator.ts`) | Auto-dispatch of autonomous task pipeline with dependency-aware staging: Planner generates plan → Implementer executes → Reviewer audits + triggers rework loop | 2026-05-11 | 2026-05-16 | DEFER | Context7 sweep (aif-primitives deep-dive 2026-05-11, 4 phrasings against `/lee-to/ai-factory`): coordinator parses inline markdown plan → builds dependency graph from `depends_on` + phase grouping → auto-dispatches subagents (parallel where deps met, sequential otherwise) → single coordinator commit per layer. In-memory state only (no external DB — unlike AIF Handoff #27/#28). Requires `claude --agent` top-level session; not callable from subagent. GAP vs our Mode B: auto-dependency dispatch, worker status tracking, circular-dep detection — all GAP cells apply only to autonomous execution (no user present). Project has no autonomous batch use case; Mode B + human-orchestrated batches adequate. We are producer for AIF, not consumer — AIF runtime adoption in our dev workflow = circular. **CRITICAL ATTRIBUTION CORRECTION 2026-05-16:** original citation `lee-to/ai-factory v2.x, subagents/implement-coordinator.md` cited a file path that **does not exist in `lee-to/ai-factory`** (confirmed via DeepWiki on ai-factory: «no `subagents/` directory exists»). Correct attribution: `lee-to/aif-handoff main, @aif/agent` — three subagents (Planner, Implementer, Reviewer) in `packages/agent/src/subagents/`, orchestrated by `coordinator.ts`. It is NOT a single «implement-coordinator» — the coordinator is `coordinator.ts` in `@aif/agent`, and the three named subagents are distinct files. Candidate description updated to reflect actual architecture. Confirmed via DeepWiki ask_question 2026-05-16 — <https://deepwiki.com/search/describe-the-planner-implement_3216df38-d6a3-42ea-935b-58abc696b2ce>. **Velocity: FAST (AIF 2.x active).** | Orchestrator runs autonomous batches ≥3 waves without user present; OR dependency ordering error (tag `#mode-b-ordering-error`) documented in ≥2 research-patches; OR Phase 11+ autonomous orchestration pipeline adopted |
| 31 | AIF `/aif` project-setup command (`/lee-to/ai-factory` SKILL.md, 2.x) | End-to-end project-aware tool bootstrapping (analyse → propose → confirm → install) | 2026-05-10 | 2026-05-10 | ADOPT | **Velocity: FAST** (AIF active 2.x; 1.x→2.x had breaking changes — mirrors entry #8). Direct end-to-end match for §13.25 user rules 1-4. Already integrated dep per [aif-comparison.md](aif-comparison.md). Building parallel surface = `#own-stack-blind-spot`. Reuse via thin wrapper (§13.25 = skill that triggers `/aif` + persistence layer for rules 5-6). | 90-day check on `/lee-to/ai-factory` per entry #8 cadence; OR AIF major version bump; OR §13.18 deep-alignment trigger fires |
| 32 | AIF skills.sh registry + skill-acquisition flow (`/lee-to/ai-factory` plan-files.md «AI Skill Acquisition Strategy») | Skill discovery + security-scanned install | 2026-05-10 | 2026-05-10 | ADOPT VOCABULARY | **Velocity: FAST** (mirrors entry #8). 5-step skill acquisition flow (search → install → security-scan → generate-if-missing → learn-from-docs) is the production-grade pattern for rule 2 («Подбор инструментов»). Vocabulary adoption documents pattern's name in any §13.25 implementation skill. | Same as #31 |
| 33 | Continue.dev `~/.continue/permissions.yaml` (continue.dev/cli/tool-permissions docs, 2026) | Persistent tool-permission learning («don't ask again») | 2026-05-10 | 2026-05-10 | DEFER | **Velocity: FAST** (Continue Hub ships frequently). Closest production analog to rule 6 (memory of accept/reject). Per-tool-call granularity — finer than rule 6's per-MCP-install granularity. Pattern adapts: replace «Allow/Ask/Exclude» with «Accepted/Rejected/Pending». | First implementation of rule 6 needs concrete schema; reuse Continue's 3-state allow/ask/exclude shape if appropriate; OR Continue formalises per-install permission model |
| 34 | Cline MCP Marketplace + `allowedMCPServers` enterprise allowlist (`/cline/cline` docs/mcp/) | Curated MCP catalogue with one-click install + enterprise gate | 2026-05-10 | 2026-05-10 | DEFER | **Velocity: FAST** (Cline ships marketplace updates monthly). Browse-driven, not proposal-driven — does not solve §13.25 rules 1-3. `allowedMCPServers` is the closest precedent for «curated MCP set» enterprise mode. | Consumer requests enterprise-allowlist mode; OR §13.25 v2 includes curation layer |
| 35 | Codex CLI `codex mcp add/list/remove` + `config/mcpServer/reload` JSON-RPC (`/openai/codex` codex-rs README) | MCP installation primitive + hot-reload | 2026-05-10 | 2026-05-10 | REJECT | **Velocity: n/a** (REJECT means re-opening requires new evidence, not just time). Pure installation primitive; identical shape to Cursor's MCP config. No project-aware proposal layer. If §13.25 implementation needs MCP install command, prefer AIF wrapper (entry #31) for stack-aware semantics. | Codex ships project-aware MCP discovery surface; OR ecosystem standardises on Codex MCP CLI shape |
| 36 | Roo Code MCP Installer (`/robertheadley/roo-code-mcp-installer`) | Programmatic MCP installation with package-manager + JSON-config layer | 2026-05-10 | 2026-05-10 | DEFER | **Velocity: UNCERTAIN** (community project, single maintainer; activity uncertain). Same shape as entry #35 — pure installation primitive — but community-driven. DEFER not REJECT because if §13.18 closes negative AND non-AIF install layer needed, this is the closest community analog. | §13.18 closes negative AND non-AIF install layer needed; OR Roo Commander absorbs the installer surface |
| 37 | Windsurf Cascade detect-and-install + MCP Marketplace deeplink (`/websites/windsurf` cascade.md, 2026) | Project-aware tool detection + MCP install (parallel to AIF `/aif` but more permissive) | 2026-05-10 | 2026-05-10 | DEFER | **Velocity: FAST** (Windsurf ships rule-system updates frequently). Second partial-end-to-end candidate alongside AIF `/aif` (entry #31). Cascade «detect, and even install» shape is more permissive than §13.25 rule 3 («never install without explicit confirmation») — install without confirmation block. Rules 5-6 absent same as AIF. DEFER not ADOPT because rule-3 conflict and §13.18 cascade-HIGH still open. | §13.18 closes negative AND non-AIF route required; OR Windsurf Cascade adds explicit batch-confirmation block for installs |
| 38 | CodeRabbit custom pre-merge checks (coderabbitai.com, SaaS) | LLM-driven PR description substance validation (§1.7 section semantic adequacy) | 2026-05-11 | 2026-05-21 | DEFER | Context7 sweep (Wave 8 research §4, query 2, 2026-05-11): `pre_merge_checks.custom_checks` with `instructions` field provides LLM-layer PR body validation matching Wave 8 Component C scope. DEFER: (a) SaaS-only, no self-hosting path; (b) ~~no deterministic paired-negative arm for judge itself~~ — **corrected 2026-05-21 (research patch 2026-05-16 §3 Q2)**: CodeRabbit DOES have deterministic paths — 15+ static-analysis linters (Ruff, ESLint, Biome, Gitleaks, Semgrep, etc.) run deterministically before LLM evaluation, AND AST-grep rules (Pro Plan) provide purely deterministic pattern-matching with no LLM. Original framing was incomplete (Context7 query keyed to `pre_merge_checks.custom_checks` missed the tool-integration + AST-grep surface; DeepWiki sweep of `coderabbitai/coderabbit-docs` surfaced it); (c) FP rate undocumented for §1.7-specific instructions; (d) ~~Component A+B (Option N deterministic baseline) covers 3/4 incidents at zero cost~~ — **stale 2026-05-13**: PR #51 is incident #5; Component A did NOT catch it (quantifier confabulation, not prose-only gap). Deterministic vacuum filled by SSOT #41 Danger JS ADOPT. Revisit when SaaS is adopted OR FP data from Component C accumulates 10+ PRs. **Velocity: FAST** (CodeRabbit ships weekly). | Project adopts CodeRabbit SaaS; OR Component C (agents/compliance-verifier.md) accumulates 10+ PR FP-rate data; OR self-hosting option announced; **Updated 2026-05-13**: deterministic alternative covered by #41 Danger JS — re-evaluate if #41 adoption stalls |
| 39 | StrykerJS (`/stryker-mutator/stryker-js`, v9.x stable 2026, `@stryker-mutator/vitest-runner`) | Mutation testing of TypeScript ESLint rule source to find surviving mutants — test-substance verification for principle + ESLint rule tests | 2026-05-12 | 2026-05-12 | ADOPT | Wave 9 R-phase D6 decision (2026-05-12): ACCEPT. Wave 9.2 I-phase installed v9.6.1 with `@stryker-mutator/vitest-runner`. Mutation run on `packages/core/eslint-rules/` yielded score 70.94% (140 killed / 54 survived / 203 total) — confirms real test-substance gaps (surviving mutants in metadata strings, AST guard conditions, FORBIDDEN_MODULES set entries). Closest alternative `jest-stryker` not applicable — project uses vitest. No other production-grade JS mutation testing framework covers vitest runner. **Velocity: STABLE** (v9.x line; major version every ~12 mo). | Stryker ships breaking change to vitest runner API; OR mutation score reaches ≥95% and maintenance cost exceeds value; OR alternative JS mutation framework adds vitest support with better coverage analysis |
| 40 | Bats Core (`/bats-core/bats-core`, v1.x) | Bash test framework with assertion helpers, structured output, parallel execution — replaces hand-rolled shell harness in `packages/core/audit-self/audit-ai-docs.test.sh` | 2026-05-12 | 2026-05-12 | WATCHLIST | Wave 9 R-phase §11 (2026-05-12): proposed for adoption. DEFERRED in Wave 9.3 — current task is single-point extension of `assert_fails` helper, not full migration. Rewrite cost (≈300 LOC across multiple test functions) exceeds value of single-test fix; Bats migration would itself become a 2-3 hour task with its own test-coverage risk. **Velocity: STABLE** (Bats v1.x line; minor releases ~2/year). | Harness test count exceeds 30+ functions AND substance assertions become majority of test bodies; OR shell test harness shows recurring `#test-asserts-only-exit-code` regressions (current incident counter: 1/3 threshold); OR project adopts other Bats users in stack |
| 41 | Danger JS (`/danger/danger-js`, v13.x stable 2026) | Deterministic programmable PR-body validation with inline GitHub feedback — machine-verifiable §1.7 forward + backward substance checking | 2026-05-13 | 2026-05-13 | ADOPT | Context7 sweep (research patch 2026-05-13 §5.1): strongest production-grade analog for PR-body validation in JS/TS domain. Direct match — same problem class, domain, paradigm. SSOT #38 CodeRabbit DEFER per no-paid-LLM creates vacuum for deterministic alternative — Danger fills it. Used by React Native, Yarn, ESLint, Apollo. Mature TS API (`danger.github.pr.body`), plugin ecosystem, `dangerfile.ts` pattern maps naturally to §1.7 schema check (Mechanism C). Verdict reversed from initial DEFER after build-vs-reuse principle challenge (maintainer 2026-05-13) — DEFER reasoning was path-of-least-resistance rationalisation against own discipline. **Velocity: STABLE** (v13.x; major release ~18 mo). | Danger major breaking change; OR maintainership disruption; OR project discontinues GitHub-based PR workflow |
| 42 | Devin/Cognition DeepWiki MCP (`mcp.deepwiki.com/mcp`, Streamable HTTP, 2025-2026) | Architectural understanding and build-vs-reuse SSOT evaluation for public GitHub repos — `ask_question` grounded in repo code; `read_wiki_structure` provides topic map; `read_wiki_contents` for full wiki pages | 2026-05-13 | 2026-05-16 | ADOPT-CONDITIONAL | Verified 2026-05-16 via experimental comparison (§3 Q1-Q3 in [research-patches/2026-05-16-research-tooling-evaluation.md](research-patches/2026-05-16-research-tooling-evaluation.md)): DeepWiki surfaces architectural depth that Context7 misses — specifically Q2 (CodeRabbit deterministic paths, verdict-relevant) and Q3 (Continue.dev precedence/argument-matching). Conditions: (1) **Public repos only** — free, no auth; private repos require Devin subscription (blocked by no-paid-LLM-in-CI policy per [.claude/rules/no-paid-llm-in-ci.md](../../.claude/rules/no-paid-llm-in-ci.md)); (2) **Coverage check mandatory** — run `read_wiki_structure` before `ask_question`; unindexed repos return «Repository not found» (kickoff §10.1 Test 2). On-demand indexing trigger via deepwiki.com URL, 10-30 min latency. (3) **Freshness caveat** — index may lag HEAD by hours-to-days; for version-specific queries, Context7 preferred (confirmed by v2 vs HEAD naming discrepancy in kickoff §10.1 Tests 3+4). T16 problem-class match STRONG: DeepWiki upstream = «understand open-source repo architecture»; our use = «build-vs-reuse SSOT evaluation for adoption candidates.» 50,000+ repos indexed. **Velocity: FAST** (Cognition/Devin active development; indexing scope and tool API evolving). | 90-day check on Devin docs (docs.devin.ai/work-with-devin/deepwiki); OR DeepWiki adds private-repo support at no extra cost (re-evaluate for Phase 10 swarming use); OR Context7 adds repo-structural-understanding mode covering architectural narratives; OR `asyncfuncai/deepwiki-open` self-hosted proves viable for private-repo SSOT evaluation |
| 43 | `lee-to/aif-handoff @aif/runtime` — RuntimeAdapter interface + Registry + Claude/Codex/OpenRouter/OpenCode adapters (aif-handoff main, `packages/runtime/`) | Provider-neutral AI runtime abstraction — unified `run`/`resume`/`forkSession` interface with transport-aware capabilities; multi-runtime dispatch without core-logic changes | 2026-05-16 | 2026-05-16 | ADOPT VOCABULARY | Verified via DeepWiki ask_question 2026-05-16 (<https://deepwiki.com/search/describe-the-aifruntime-packag_ef408848-368e-4d4a-870e-af9d51c1567f>): `RuntimeAdapter` interface defines `run(input)`, `resume(input)`, `forkSession(input)`, `listModels(input)`, `validateConnection(input)`, `getEffectiveCapabilities(transport)`. Registry bootstrapped via `bootstrapRuntimeRegistry`; supports external modules via `AIF_RUNTIME_MODULES` env var. Built-in adapters: Claude (SDK/CLI/API), Codex (SDK/CLI/App Server/API), OpenRouter (API), OpenCode (API). T16 problem-class match: their problem = «abstract multiple LLM providers behind a unified runtime contract for an autonomous task pipeline»; our problem = «provide AI-agnostic sub-agent prompts callable from any AI tool» — **different abstraction layers**. Their layer is full-runtime (transport + capabilities + usage tracking); ours is markdown-prompt (no runtime code). ADOPT VOCABULARY: validates our AI-agnostic sub-agent boundary discipline empirically as a subset of the broader «provider-neutral runtime» pattern. Gives maturity reference if/when `agents/*.md` pattern evolves to need full runtime abstraction. **Velocity: FAST** (AIF Handoff active development). | `agents/*.md` pattern evolves beyond markdown prompts to need runtime-level invocation or multi-transport support; OR Phase 11+ AIF integration formalises runtime layer; OR project adopts @aif/runtime as a direct dependency |
| 44 | `lee-to/aif-handoff @aif/mcp` — MCP server for Task/Plan management with bidirectional status sync (aif-handoff main, `packages/mcp/`) | MCP server exposing task CRUD + plan management + `handoff_sync_status` bidirectional sync — any Claude Code session can manage autonomous tasks via MCP tools | 2026-05-16 | 2026-05-16 | DEFER | Verified via DeepWiki ask_question 2026-05-16 (<https://deepwiki.com/search/describe-the-aifmcp-package-wh_3bda4062-8f45-4055-a316-0f371ed311a2>): tools include `handoff_create_task`, `handoff_update_task`, `handoff_get_task`, `handoff_list_projects`, `handoff_list_tasks`, `handoff_search_tasks`, `handoff_annotate_plan`, `handoff_push_plan`, `handoff_sync_status`. Bidirectional sync uses last-write-wins conflict resolution; terminal states (`done`/`verified`) guarded from AI-side override; `paused` flag atomically settable with status. Supports stdio + HTTP transport. T16 problem-class: their problem = «external AI agents coordinate tasks via MCP tools in an autonomous kanban pipeline»; our use case = «human-orchestrated swarm sessions need shared task state». **Match is CONDITIONAL** — if Phase 10 swarm pattern adopts kanban-shape for task tracking, @aif/mcp is a direct adoption path. Currently DEFER because: (1) project has no autonomous batch use case (human orchestrator present); (2) adoption requires standing up aif-handoff infrastructure (Docker, DB) — overhead at current interactive scale; (3) Phase 10 swarm design not yet finalised. MIT licensed. **Velocity: FAST** (AIF Handoff active development). | Phase 10 swarm pattern adopts kanban-shape requiring shared task state across parallel sessions; OR ≥3 waves require cross-session coordination with state that can't be tracked via research-patch files; OR aif-handoff project becomes the official Phase 11+ orchestration layer |
| 45 | `lee-to/aif-handoff @aif/agent` — Watchdogs, Error Classification & Recovery (aif-handoff main, `packages/agent/src/taskWatchdog.ts` + `stageErrorHandler.ts` + `errorClassifier.ts`) | Self-healing pattern for autonomous pipelines: First-Activity Watchdog + Stale-Stage Watchdog + error classification + exponential-backoff retry with quarantine cap | 2026-05-16 | 2026-05-16 | ADAPT | Verified via DeepWiki ask_question 2026-05-16 (<https://deepwiki.com/search/describe-the-watchdogs-error-c_11b95c86-96e3-4879-980b-85a882d538a3>): three watchdog layers — (1) First-Activity Watchdog (60s timeout on first tool call / subagent spawn; detects «zombie» sessions); (2) Query Startup Guard (60s for first WebSocket/HTTP stream message, one retry); (3) Stale-Stage Watchdog (background scan for tasks stuck >90m without heartbeat; max 3 retries then quarantine). Error classifier (`classifyStageError`) handles `BranchIsolationError`, `RuntimeExecutionError` categories (`rate_limit`, `auth`, `timeout`, `model_not_found`, `context_length`), fast-retry transient patterns. Recovery: `releaseDueBlockedTasks` with random backoff 5-15 min. T16 problem-class: their problem = «autonomous pipeline self-heals when a subagent session hangs»; our current pattern = «parallel-subwave-isolation.md: STOP and report to orchestrator on failure». **ADAPT verdict**: their pattern solves our documented orchestrator-stuck problem (Wave 8.1/8.1b contamination — worktree-add failure scenario). Adaptation: orchestrator-side self-healing for long-running waves by classifying session failures and retrying with backoff vs escalating to human. Requires state machine formalization (currently we have none). **Velocity: FAST** (AIF Handoff active development). | ≥3 wave incidents document «orchestrator stuck because Sonnet session hung» (currently 1: Wave 8.1/8.1b/8.2 contamination 2026-05-12); OR parallel-subwave-isolation.md rule evolves beyond «STOP and report» to include retry logic |
| 46 | `lee-to/aif-handoff` Subagents-mode vs Skills-mode execution dichotomy (`AGENT_USE_SUBAGENTS` env var; `executionMode: "native_subagents"` vs `"standard"`) | Two productized execution modes: Subagents (iterative, parallel workers, higher quality, more tokens) vs Skills (single-pass, faster, fewer tokens) — direct analog of our Mode A/B orchestrator pattern | 2026-05-16 | 2026-05-16 | ADOPT VOCABULARY | Verified via DeepWiki ask_question 2026-05-16 (<https://deepwiki.com/search/what-are-the-two-execution-mod_6ac0cfd0-3e83-4db7-8152-c67c9a7af074>): Subagents mode (`AGENT_USE_SUBAGENTS=true`) uses `executionMode: "native_subagents"` — Planner/Implementer/Reviewer subagents with iterative refinement, parallel workers, rework loops; higher quality, more tokens. Skills mode (`AGENT_USE_SUBAGENTS=false`) uses `executionMode: "standard"` with `fallbackSlashCommand` (e.g. `/aif-plan`, `/aif-implement`) — single-pass, faster, fewer tokens. A third intermediate mode `"isolated_skill_session"` exists for environment isolation. T16 problem-class: their dichotomy = «autonomous task pipeline quality/speed tradeoff»; our Mode A/B = «orchestrator session scope: single-concern (A) vs parallel-batch (B)». **Different dimensions** — their axis is quality/speed of execution; ours is session-scope isolation strategy. Nevertheless ADOPT VOCABULARY: the Subagents/Skills terminology is the productized, industry-validated name for the iterative-vs-single-pass pattern our orchestrator Mode A/B implements. Vocabulary alignment improves cross-reference with aif-handoff documentation. **Velocity: FAST** (AIF Handoff active development). | Orchestrator skill is rewritten or replaced with aif-handoff-based runtime (Phase 11+ scenario); OR Mode A/B pattern diverges sufficiently from Subagents/Skills that vocabulary alignment misleads rather than clarifies |
| 48 | Principle 11 executable test (`packages/core/principles/11-build-first-reuse-default.test.ts`) — BFR-default invariant mechanically enforced via capability-file enumeration + SSOT-verdict check + Prior-art-trailer check | CI-enforced aggregate-state audit: every post-grandfather capability artifact (`.claude/rules/*.md`, agents, skills, non-test TS ≥50/80 LOC) must have a BFR SSOT entry or valid Prior-art trailer | 2026-05-17 | 2026-05-17 | BUILD | Companion principle test for SSOT #47 (BFR rule). DeepWiki probes 2026-05-13 (same session as #47) confirmed no upstream candidate for «capability-artifact-to-SSOT provenance enforcement via CI principle test». Problem-class is narrow: enforce SSOT-verdict presence for own project artifacts, not general mutation-testing or rule-enforcement frameworks. Sibling principle tests (08–12) are BUILD-class by the same reasoning: the framework tests its own invariants on its own artifact set. Path: `packages/core/principles/11-build-first-reuse-default.test.ts`. | Upstream «rules-as-tests» framework ships equivalent artifact-provenance enforcement (currently: no upstream candidate per SSOT #47 research); OR companion-integration-analysis R-phase surfaces a candidate with >80% problem-class overlap |
| 47 | Macro-level scope-discipline rule (BFR-default operating philosophy) — negative-existence finding from DeepWiki 2026-05-13 probes (ai-factory 0/7 enforcement patterns; aif-handoff 3/7 different application surface; OhMyOpencode tool-restrictions-as-permissions different shape) | Macro-level scope discipline complementing per-commit Phase 8.8 invariant — default verdict for capability proposals across 3-6-month composition windows; mechanism per `.claude/rules/build-first-reuse-default.md` §3 (DeepWiki + WebSearch + SSOT consult + this rule itself) | 2026-05-13 | 2026-05-16 | BUILD | DeepWiki probes 2026-05-13 (recorded in [research-tooling-evaluation/kickoff.md §10](../../.claude/orchestrator-prompts/research-tooling-evaluation/kickoff.md)) surveyed three closest upstream candidates at the macro-discipline granularity: ai-factory (workflow framework, no scope-discipline rule); aif-handoff (orchestration framework, lint-staged+husky at micro level only, no macro-level rule equivalent); OhMyOpencode (tool-restrictions-as-permissions, different shape). None match the problem-class «default-adoption + escape-hatch rationale for capabilities composed at quarterly scale on single-maintainer projects». BUILD verdict per rule's own §1 typology applied recursively. **WebSearch on «macro-level scope discipline operating principle» NOT YET EXECUTED** in 1A session (T-GC-F scope constraint kept it out — see [research-patches/2026-05-16-goal-clarity-dialogue.md §11.2](research-patches/2026-05-16-goal-clarity-dialogue.md)). Flagged as residual gap; trigger revisit if WebSearch surfaces candidate. Recursive self-application: this very SSOT entry records the rule's first application of itself. | WebSearch ≥3 phrasings on «macro-level scope discipline» surfaces an upstream candidate; OR alt-target comparison research (Superpowers / OhMyOpencode / Agent Teams / Cline / Cursor / Codex / Aider) reveals a peer with equivalent macro-discipline rule; OR principle test 11 ships and shifts the discipline's enforcement layer from prose to executable |
| 49 | `multica-ai/andrej-karpathy-skills` (MIT, 2026) — Karpathy LLM-coding-pitfall guidelines shipped as Claude Code skill (`skills/karpathy-guidelines/SKILL.md` + `plugin.json`) + `CLAUDE.md` + Cursor `.mdc` rule | Prose behavioral coding-discipline guidelines for AI agents (four principles: Think-Before-Coding, Simplicity-First, Surgical-Changes, Goal-Driven verification) | 2026-05-20 | 2026-05-20 | DEFER | User-surfaced 2026-05-20 (WebFetch + DeepWiki ask_question on `multica-ai/andrej-karpathy-skills`). BFR-default (`.claude/rules/build-first-reuse-default.md`) classification = **REFERENCE / KEEP NARROW**: all four principles already covered by existing discipline — Surgical-Changes ↔ CLAUDE.md PR no-drive-by + `ai-laziness-traps` T5 (no fixes in R-phase); Think-Before-Coding ↔ `phase-research-coverage` §1 + `reviewer-discipline` §2 surface-as-decision; Simplicity-First ↔ `build-first-reuse-default` (#own-stack-blind-spot, anti-over-engineering); Goal-Driven test-first ↔ project core thesis (rules-as-executable-artifacts). It is the prose layer our «documents lie; tests don't» thesis builds *atop* — adopting as a skill would duplicate Class-C prose with no executable companion (`#pattern-matching-on-name`: «karpathy skills» sounds authoritative but is the prose layer, not a tool). No dep / code / vocabulary adoption. **Velocity: UNCERTAIN** (single-author guideline repo; no release cadence signal). | A coding-discipline skill/rule is proposed in this problem-space (use these four principles as a content/wording source, re-evaluate ADAPT); OR the repo grows an executable-enforcement layer (test/hook) that our prose rules lack |
| 50 | AI Factory `skill-context` mechanism (`.ai-factory/skill-context/<skill>/SKILL.md`, v2.11.0) — every aif quality skill MANDATORY-reads it; `aif-evolve` writes there; documented as a "fixed internal AI Factory path" | AIF-native "extend/override a vendored sub-agent without occupying or overwriting its agent slot" — inject project-specific rules into AIF's own skills + background sidecars (`aif-review`, `aif-rules-check`) | 2026-05-20 | 2026-05-20 | ADOPT | C-1 agent-collision follow-up ([research-patches/2026-05-20-agent-collision-resolution.md](research-patches/2026-05-20-agent-collision-resolution.md) §W6) — the delivery channel PR #79 deferred pending the live probe. **Live probe 2026-05-20:** a `background:true, maxTurns:6` `review-sidecar` **reads + applies** skill-context (3/3 read, 2/2 apply with legitimately-phrased conventions; injection-shaped "OVERRIDE/token" text correctly refused). Lets us deliver `review-sidecar` anti-tautology content + the removed `best-practices-sidecar`'s R10/test-existence residue into AIF's pipeline occupying **zero** colliding agent slots, version-bump-robust (fixed path). T16 problem-class match STRONG: upstream = "project-specific override of a vendored skill"; ours = identical. No dep added; rides existing AIF substrate (`install.sh` wraps `ai-factory init`). | AIF removes/renames the skill-context path or changes its MANDATORY-read semantics; OR Claude Code ships first-class partial-override of a vendored sub-agent ([anthropics/claude-code#25546](https://github.com/anthropics/claude-code/issues/25546)), making the skill-context indirection unnecessary |
| 51 | Specification gaming examples catalogue (Victoria Krakovna / DeepMind, 2018–ongoing, [blog](https://vkrakovna.wordpress.com/2018/04/02/specification-gaming-examples-in-ai/)) | Canonical vocabulary for the `#discipline-theatre` antipattern — «satisfying the literal specification without achieving the intended outcome» | 2026-05-12 | 2026-05-21 | ADOPT VOCABULARY | Wave 9 §5.0 WebSearch (first external literature sweep in project history). The specification-gaming definition and community-curated example catalogue is the academic lineage for `#discipline-theatre`. No code adoption — vocabulary only: feeds `phase-research-coverage.md §4` anti-pattern description. **Velocity: LOW** (academic definition stable). Landed 2026-05-21 via Wave 9 R-phase rescue (orphaned branch `docs/wave-9-theatre-audit-r-phase`; draft #39 → final #51). | Anthropic or DeepMind publishes a successor paper extending specification gaming to dev-tooling / CI contexts |
| 52 | CodeRabbit policy-as-code four-layer governance model (coderabbit.ai/blog/policy-as-code…, May 2026) | Industry-canonical vocabulary for what this project implements: principles → rules → automated checks → escalation paths | 2026-05-12 | 2026-05-21 | ADOPT VOCABULARY | Wave 9 §5.0 WebSearch. Four-layer model and «policy-as-code» / «compliance-as-code» are industry-canonical framing for this project's architecture. «Visible exception tracking with justification, approval, and time-bounds» maps to the escape-hatch pattern. Strengthens INSTALL-FOR-AI.md / consumer onboarding. Distinct from entry #38 (SaaS tooling vs vocabulary). **Velocity: FAST** (CodeRabbit ships weekly). Landed 2026-05-21 via Wave 9 R-phase rescue (draft #41 → final #52). | CodeRabbit ships a self-hostable policy-as-code CLI that could replace or complement existing hook infrastructure |
| 53 | promptfoo (`promptfoo/promptfoo`) + Inspect (`UKGovernmentBEIS/inspect_ai`) + METR transcript-analysis (metr.org 2026-02) — behavioural-eval harness for «did an intervention change agent behaviour» | Session-bound deterministic eval over agent transcripts: claim→verification groundedness scorer ([tests/eval/](../../tests/eval/)) | 2026-05-21 | 2026-05-21 | BUILD | (Upstream harnesses = REFERENCE precedent; the operative decision is BUILD-ours, parallel to entry #16.) Instruction-compliance-empirical R-phase §5 (DeepWiki 2 repos + WebSearch ≥3). **DeepWiki-confirmed:** promptfoo supports purely deterministic assertions (`regex`/`javascript`/`python`/`trajectory:tool-used`) and scores **pre-existing outputs** via the `echo` provider / `runAssertions()` — no provider API; Inspect's re-scoring workflow ingests **external** `EvalSample.output` and scores with code graders, no model call. Both host the *grading* half; **neither hosts the no-paid-LLM *generation* half** (operator's CC subscription session is unmovable into either). METR «analyzing coding agent transcripts» = observational-methodology precedent (+ its RCT-validation caveat). **T16 problem-class check:** upstream = drive-generation-then-grade; ours = grade-pre-existing-real-transcripts-with-no-paid-generation → match on grading, miss on generation → **BUILD the thin ~190-LOC CC-`.jsonl` scorer**, **REFERENCE** the deterministic-grader-on-pre-existing-transcripts pattern + **ADOPT VOCABULARY** `trajectory:tool-used`. Scorer is session-bound (not CI — no-paid-LLM + private transcripts). | A JS/CC-native eval framework ingests CC `.jsonl` + scores deterministically with less integration than the hand-rolled scorer; OR the scorer must grow beyond grep (then re-evaluate Inspect re-scoring) |
| 54 | Aider `Linter.run_cmd` / `Linter.lint` (`Aider-AI/aider`, aider/linter.py) — testable external-command runner: invoke a command, capture exit code + stdout + stderr, unit-tested by mocking the subprocess boundary | Wave 10.1 pre-push migration — §1–§6/§8 delegation sections call one unit-tested `runCheck()` ([packages/core/hooks/utils/run-check.ts](../../packages/core/hooks/utils/run-check.ts)) instead of un-tested `execSync` shell-outs | 2026-05-21 | 2026-05-21 | ADAPT | Research patch [§4.8.X.1](research-patches/2026-05-16-§13.33-hook-architecture-research.md) (DeepWiki `Aider-AI/aider` ×3 phrasings, 2026-05-21). **T16 problem-class check:** upstream = run lint/test then feed failures into an LLM fix-loop; ours = run pre-push external checks and route on exit code → **match** on the runner-abstraction + exit/output-capture dimension, **miss** on the LLM-fix-loop (a pre-push gate blocks, never auto-fixes) → ADAPT the tested `run_cmd` slice, drop the `Coder` fix-loop. Mocked-subprocess test strategy adopted (à la Aider's `@patch("subprocess.Popen")`); **timeout case added beyond upstream** (Aider omits it) so coverage strictly exceeds — run-check.ts 90% mutation score vs the bash predecessor's 0%. Shipped PR #107. | A maintained TS/JS pre-push command-runner with timeout + exit/output capture surfaces upstream (re-evaluate ADOPT); OR `run-check.ts` grows past thin delegation into fix-loop / orchestration logic |
| 55 | Superpowers (`obra/superpowers`) TDD-for-Skills discipline — «NO SKILL WITHOUT A FAILING TEST FIRST» / RED-GREEN-REFACTOR for skill authoring | Paired-negative enforcement extended from code-rule artifacts (principle 02) to skill artifacts (`SKILL.md`): a documented without-skill failure paired with the with-skill correction, checked structurally | 2026-05-21 | 2026-06-01 | ADAPT | N2 #5 ([research-patches/2026-05-21-n2-adopt-from-superpowers.md §3 row 5](research-patches/2026-05-21-n2-adopt-from-superpowers.md), maintainer DECISION=C). **T16 problem-class:** upstream = RED-GREEN-REFACTOR *process* for skill authoring; ours = re-express the paired-negative *idea* as an executable structural check on `SKILL.md` (substrate). Match on idea, not mechanism → ADAPT, not ADOPT. **Substrate-pure:** zero Superpowers dependency (C invariant — `package.json` companion-dep grep stays empty). New slot 15 (not an extension of principle 02 — `SKILL.md` has no `examples.bad/good` fields). Shipped as [packages/core/principles/15-skill-paired-negative.test.ts](../../packages/core/principles/15-skill-paired-negative.test.ts) (body-section marker, `EXEMPT_SKILLS` allowlist grandfather). | Superpowers ships a language-agnostic skill-test format adoptable verbatim; OR a skill-authoring pain incident motivates the N2 #6 pressure-scenario probes ([open-questions.md §13.37](open-questions.md)) |
| 56 | §7 Prior-art trailer parser (pa_* functions, `packages/core/hooks/checks/prior-art.ts`) — capability-commit detection (new dep / ≥50 LOC new core subdir / ≥80 LOC new packages/ file) + `Prior-art:` trailer validation (presence, length, escape-hatch substance arm) | Wave 10.2 — port `pa_is_new_dep_added`, `pa_is_new_core_subdir_50loc`, `pa_is_new_packages_80loc`, `pa_detect_capability_reason`, `pa_check_trailer` from bash shim to typed TS with injected GitProvider; Stryker ≥80% (D4 gate) | 2026-05-21 | 2026-05-21 | BUILD | Research patch [§5.1 Branch (c) «trivial-regex-port / own-build TS»](research-patches/2026-05-16-§13.33-hook-architecture-research.md) + §5.2 D6 «own-build core … remains BUILD because no upstream solves that slice». DeepWiki probes in §4.1–§4.6 surveyed lint-staged, husky, Lefthook, simple-git-hooks, Danger JS, CodeRabbit — none implement capability-commit detection (new dep key presence + LOC threshold checks) combined with a `Prior-art:` trailer format validation with escape-hatch substance arm. **T16 problem-class check:** upstream pre-push frameworks = «run external linters/formatters on changed files»; our problem-class = «detect if a commit introduces a new build capability (LOC/dep heuristic) AND verify a project-specific trailer discipline exists» — structural mismatch. No upstream candidate solves the compound problem; the three adopted patterns (SSOT #54 runner, §4.8.X.2 check-registry, §4.8.X.3 changed-file scoping) enrich *how* the code is structured, not *whether* to build. Stryker 84.07% on first run (≥80% D4 gate passed). Dogfooding: this commit's own `Prior-art:` trailer runs through the ported TS logic. | A pre-push framework ships «capability-commit detection + project-specific trailer validation» as a declarative config (current: no candidate); OR the bash predecessor re-surfaces a bug the TS port doesn't reproduce (re-evaluate heuristic logic) |
| 57 | §1.7 discipline-trailer parser (s17_* functions, `packages/core/hooks/checks/s17.ts`) — detect rule/principle/SKILL-introducing commits (file-glob + new `## §`/`export const` section marker, D3 allow-list) + validate the `§1.7:` forward/backward trailer (Bootstrap exemption, Wave 8.3 file:line substance arm, Wave 9.4 body-prose detection) | Wave 10.3 — port `s17_is_discipline_introducing`, `s17_check_trailer` from the (now deleted) legacy-trailer-checks.sh to typed TS with injected GitProvider; 11 bash scenarios ported verbatim to vitest; Stryker ≥80% (D4 gate) | 2026-05-21 | 2026-05-21 | BUILD | Same problem-class + verdict as its §7 sibling SSOT #56: research patch [§5.1 Branch (c)](research-patches/2026-05-16-§13.33-hook-architecture-research.md) + §5.2 D6 own-build. **T16 problem-class check:** upstream pre-push frameworks (lint-staged / husky / Lefthook / Danger / CodeRabbit, surveyed §4.1–§4.6) run linters on changed files; our problem-class = «detect a discipline-introducing commit by file-glob + section-marker AND validate a project-specific §1.7 forward/backward trailer with bootstrap + substance + prose arms» — no upstream solves it. Completes the trailer migration: legacy-trailer-checks.sh + pre-push.test.sh deleted, no bash trailer logic remains. Stryker 82.52% (≥80% D4). Dogfooding: pre-push §1.7 now runs through this TS module. | A pre-push framework ships project-specific discipline-trailer validation as declarative config (none today); OR the deleted bash re-surfaces a bug the TS port doesn't reproduce |
| 58 | remark (`remarkjs/remark`) + unified (`unifiedjs/unified`) + mdast — Markdown AST parser for Node.js; code-fence-aware text extraction (code blocks = type `code`, inline code = type `inlineCode`, prose = everything else) | Wave 10.4 — D1 probe hardening in `audit-ai-docs.ts`: extract skill names from AGENTS.md prose only, skipping fenced code blocks and negative-mention context (bash awk/grep could match inside code blocks and false-positive on "removed skill `x`") | 2026-05-21 | 2026-05-21 | ADOPT | Prior-art Step 0 (dispatch §STEP 0, 2026-05-21): DeepWiki `remarkjs/remark` + `unifiedjs/unified` ×2 phrasings + WebSearch ×3 phrasings on "markdown AST parser node code-fence-aware text extraction". **Candidates evaluated:** (1) **remark/unified** — AST-based; `code`/`inlineCode` nodes distinct from `text` nodes; `unist-util-visit` traversal; production-grade (remark@15, 2026); T16 problem-class match STRONG: «code-fence-aware markdown prose extraction in Node.js» = exact upstream problem class. (2) **markdown-it** — token-stream, HTML-output focus; no first-class AST visitor; harder to skip code nodes cleanly. (3) **mdast-util-from-markdown** (lower-level) — remark wraps this; no reason to depend on lower level directly. (4) **marked** — HTML-output only, no AST. Verdict: ADOPT remark as the abstraction layer; `remark-parse` (included) + `unist-util-visit` (bundled with remark) provide all needed primitives. Zero custom parser code. **T16 check:** upstream = «parse markdown into structured AST»; ours = «extract prose text skipping code nodes» — exact sub-problem of upstream problem class → match, not mismatch. No architectural overhead: `remark().parse(md)` + `visit()` is 5 LOC. | remark is abandoned or breaks ESM compatibility; OR a lighter-weight AST-only package (e.g. `micromark` + hand-rolled visitor) proves cheaper to maintain; OR D1 probe is removed (skill-drift check migrates to a different mechanism) |
| 59 | Aider `parse_lint_cmds` / `self.languages` (`Aider-AI/aider`, aider/main.py + aider/linter.py) — declarative language→command mapping; per-language linter selection decoupled from `run_cmd` execution; intentionally separated per DeepWiki: «The per-language mapping and the execution logic are intentionally separated so users can configure commands independently of how Aider runs them» | Wave 10.5 — `packages/core/hooks/checks/registry.ts`: declarative check-registry (`{ id, criticalForFallback, runner }[]`) decoupling check-set *selection* (critical-for-bash-fallback vs TS-core-only) from check *execution*; consumed by `registry.test.ts` (bash-expressible invariant) and `pre-push.fallback.sh` (critical-only bash gate) | 2026-05-22 | 2026-05-22 | ADAPT | Research patch [§4.8.X.2](research-patches/2026-05-16-§13.33-hook-architecture-research.md) (DeepWiki `Aider-AI/aider` ×3 phrasings, 2026-05-21). **T16 problem-class check:** upstream problem class = «select a linter per source-file language»; our problem class = «select a check-set per consumer stack (TS-core full vs bash-fallback critical-only)». **Match** on the *decouple-selection-from-execution* structure; the declarative selection-table-consulted-by-generic-runner pattern transfers. **Mismatch** on the semantic axis (file-language vs project-stack) — no over-claim of equivalence (T16 countermeasure applied). ADAPT (not ADOPT): our registry axis is stack/capability, not language. No new npm dependency (hand-written TS data table). Unit-test mandate from §4.8.X.2 applied: `registry.test.ts` asserts «every criticalForFallback check is bash-expressible» (runner: 'bash') — the invariant is itself tested. registry.ts is 114 LOC (≥80 LOC under `packages/` → this is a capability commit; SSOT entry required). | Aider ships a generic multi-stack pre-push check-registry framework (currently it is lint-only, file-language scoped); OR the registry grows past pure-data into control-flow (at which point extract to a separate module and re-evaluate) |
| 60 | Agent RuleZ (Hightower, Feb 2026) — "deterministic policy engine in human-readable YAML, evaluation in Rust, sits on top of Claude Code's hooks protocol"; directories matcher (`src/api/**` glob) + per-language extensions matcher; "files outside those directories do not trigger the rule" | Channel-selection wave (`.claude/rules/rule-enforcement-channel-selection.md`) — production-grade articulation of the "scoped conditional injection on CC hooks" delivery channel our two-axis principle defines | 2026-05-22 | 2026-05-22 | REFERENCE | Channel-selection research patch [§2 candidate #5](research-patches/2026-05-22-rule-enforcement-channel-selection.md) (WebSearch ×3, 2026-05-22: medium.com/@richardhightower agent-rulez + "injecting the right rules at the right moment"). Independent articulation of our exact principle = convergent-evolution evidence it is sound. **NOT ADOPT:** adds a Rust runtime dependency for glob/extension-scoped injection that CC hooks already provide natively (SSOT #20 ADOPT) — single-maintainer maintenance cost per [build-first-reuse-default.md §2](../../.claude/rules/build-first-reuse-default.md). **T16 check:** name "policy engine / matchers" pattern-matches to "adopt"; verified the capability (path-scoped JIT injection) is native to CC hooks → REFERENCE, not ADOPT. **INCONCLUSIVE** on license/maturity: Medium full text paywalled, claims from search snippet only. | Agent RuleZ open-sources under a permissive license AND a matcher class we need proves absent from CC hooks; OR a 2nd channel-selection incident shows our hand-rolled path-scoped injector under-delivers vs a policy-engine |
| 61 | OhMyOpencode hook system (`code-yeongyu/oh-my-openagent`) — `rulesInjector` (injects `.claude/rules/` / AGENTS.md by glob pattern + file path, tracks read/write/edit/multiedit on `tool.execute.after`) + `writeExistingFileGuard` (mechanical tool-guard on `tool.execute.before`) | Channel-selection wave — the path/glob-scoped JIT-injection channel + mechanical-gate rung, on a CC-compatible (OpenCode) hook substrate; adapted by the shipped `.claude/hooks/inject-matching-rule.sh` | 2026-05-22 | 2026-05-22 | ADAPT | Channel-selection research patch [§2 candidate #4 + §4](research-patches/2026-05-22-rule-enforcement-channel-selection.md) (DeepWiki ×3, 2026-05-22). Verdict ADAPT (we shipped an adaptation); REFERENCE precedent for the mechanical-gate rung noted below. **The single richest match:** `rulesInjector` (glob → inject only the `.claude/rules/` path touched) is almost exactly our principle's "context-triggered injection scoped to path"; `writeExistingFileGuard` demonstrates the mechanical-gate rung. **ADAPT:** `.claude/hooks/inject-matching-rule.sh` (PostToolUse path-scoped rule-injector, ≤60 LOC deterministic bash, no paid LLM per [no-paid-llm-in-ci.md](../../.claude/rules/no-paid-llm-in-ci.md)) adapts `rulesInjector` to CC's `PostToolUse` + a `<!-- globs: -->` front-matter convention, generalising our existing [check-doc-authority.sh](../../.claude/hooks/check-doc-authority.sh) internal path-filter. **T16 check:** STRONG match (path/glob *deterministic* injection = our reliability-of-load-bearing-delivery class). Not full ADOPT — importing OhMyOpencode = an OpenCode runtime dependency for a capability we build in ≤60 LOC bash. | OhMyOpencode ships a CC-native (not OpenCode) distribution AND our hand-rolled injector proves insufficient; OR the `<!-- globs: -->` marker convention spreads to ≥3 rules warranting a richer matcher engine |
| 62 | Cursor rule types — canonical breadth taxonomy: **Always** (`alwaysApply:true`, every request) / **Auto-Attach** (`globs`, only when a matching file is in context) / **Agent** (`description`, model-requested, best-effort) / **Manual** (explicit ref only) | Channel-selection wave — the breadth-axis vocabulary; maps 1:1 to our delivery ladder (always-on / path-scoped / semantic / explicit) | 2026-05-22 | 2026-05-22 | ADOPT VOCABULARY | Channel-selection research patch [§2](research-patches/2026-05-22-rule-enforcement-channel-selection.md) (WebSearch 2026-05-22, cursor.com/docs/rules). Maps 1:1 to our breadth ladder; no runtime coupling. **Distinct from existing SSOT #13** (Cursor Team/Project/User *precedence tiers*, DEFER) — that is the precedence facet; #62 is the four *rule-activation types* → not a duplicate. **Community gap:** Cursor globs trigger on *editor tabs*, not *AI file access* ([forum #156553](https://forum.cursor.com/t/glob-scoped-rules-should-trigger-on-ai-file-access-not-just-editor-tabs-plus-mcp-cli-tool-triggers/156553)) → OhMyOpencode (#61) `tool.execute.after` model is strictly better for agents. We adopt the *vocabulary*, not Cursor's trigger mechanism. | Cursor changes its rule-type taxonomy in a way that diverges from our breadth ladder; OR our ladder gains a 5th delivery class needing a vocabulary anchor |
| 63 | agent-situations (`dave1010`) — `.situations/` directory; each Situation carries a `check` (file-exists / regex / exit-code); if the check passes, its context is appended to the system prompt | Channel-selection wave — ephemeral check-gated injection precedent ("deterministic check passes → inject context") paralleling our PostToolUse path-match injector | 2026-05-22 | 2026-05-22 | REFERENCE | Channel-selection research patch [§2](research-patches/2026-05-22-rule-enforcement-channel-selection.md) (WebSearch 2026-05-22, github.com/dave1010/agent-situations). Check-gated injection precedent. **REFERENCE only:** no new mechanism vs our shipped injector + CC hook primitives (SSOT #20) — a separate `.situations/` runtime would duplicate what CC hooks give natively. **T16 check:** "situations/checks" ≠ adopt — the deterministic-check-then-inject capability is already covered by our PostToolUse hook. | agent-situations matures into a CC-compatible distribution AND offers a check class (beyond file-exists / regex / exit-code) our injector lacks |
| 64 | Superpowers (`obra/superpowers`) `subagent-driven-development` (SDD) skill — coordinator dispatches a fresh implementer subagent per task, then **two separate fresh reviewer subagents** (`spec-reviewer-prompt.md` → `code-quality-reviewer-prompt.md`), continuous execution, no npm dep, dispatched via the harness's native subagent capability (not headless `claude -p`) | N7 process-layer dogfooding — subagent orchestration in *our own dev* (mirrors orchestrator Mode A dispatch + Worker→Reviewer cycle + Phase -1 cold-review). Process layer only: installs into the maintainer's global `~/.claude/`, **not** shipped via `install.sh`; substrate stays dependency-free (C-guard) | 2026-05-22 | 2026-05-22 | ADOPT | N7 research patch [§2 row 1 + §6](research-patches/2026-05-22-n7-dogfood-companions.md), DECISION=C axis-B. **Verified against shipped source (not assumed):** SDD `SKILL.md` two-stage review = *separate* fresh subagents per stage (dual-channel DeepWiki + raw `raw.githubusercontent.com/obra/superpowers/main/skills/subagent-driven-development/SKILL.md`, 2026-05-22) — resolves the patch §7(b) INCONCLUSIVE. **T16 problem-class check:** upstream = coordinator dispatches isolated subagents per task with two-stage post-implementation review; ours = orchestrator umbrella decomposition + per-task dispatch + review → **match on the inner dispatch+review loop**, **miss on** orchestrator-unique meta-layer (quota zones, Mode A/B economics, Phase -1 *pre*-dispatch kickoff review, bootstrap discovery) → ADOPT for the inner loop, KEEP-NARROW orchestrator for the meta-layer (retention verdict A coexist). **N0 relief:** native-subagent dispatch (not `claude -p`) keeps SDD runtime off the post-2026-06-15 Agent-SDK credit pool. Substrate-pure: `package.json` companion-dep grep stays empty. Distinct from SSOT #55 (TDD-for-Skills = ADAPT into our principle *tests* = substrate). | SDD ships a breaking dispatcher-contract change; OR a live-dogfood trial (patch §4 step 5, pending global install) surfaces Phase -1 incompatibility; OR SDD's own headless-`claude` *test* harness is mistaken for a runtime dep and pulled into CI (would violate no-paid-llm-in-ci) |
| 65 | Superpowers (`obra/superpowers`) `using-git-worktrees` skill — pure-git worktree isolation; Step 0 detects `GIT_DIR != GIT_COMMON_DIR` (with submodule guard) and **skips nested creation**, Step 1b verifies `.worktrees/` gitignored before creating, Step 4 runs a baseline test on consent; no npm dep, no headless `claude` | N7 process-layer dogfooding — worktree isolation in *our own dev*; the preventive enforcement primitive that [parallel-subwave-isolation.md §4](../../.claude/rules/parallel-subwave-isolation.md) now REFERENCES instead of building (AST-detection ambition dropped) | 2026-05-22 | 2026-05-22 | ADOPT | N7 research patch [§2 row 2 + §6](research-patches/2026-05-22-n7-dogfood-companions.md), DECISION=C axis-B. **Verified against shipped source (not assumed):** the `GIT_DIR != GIT_COMMON_DIR` Step-0 skip + the consent-gated Step-4 baseline test confirmed via dual-channel DeepWiki + raw `.../skills/using-git-worktrees/SKILL.md`, 2026-05-22 — resolves patch §7(c). **T16 problem-class check:** upstream = `.git/index`-race avoidance via worktree-per-task with already-isolated detection; ours = the *exact same* race (incident 2026-05-12, Wave 8.1/8.1b) → STRONG match. Crucially Step-0 detection makes it compatible with Claude Code `isolation:"worktree"` (skips rather than nests) — the §3 critical-compatibility finding, now source-verified (T16 countermeasure satisfied). ADOPT (process layer) + REFERENCE in the rule. Substrate-pure (no `install.sh` ship, no dep). **Residual (patch §7a):** Step-4 baseline `npm test` in this repo triggers the full vitest+principle suite (slow, not corrupting; consent-gated) — confirm in the pending live trial. | `using-git-worktrees` worktree-detection becomes CC-incompatible; OR the rule's retirement criterion (12 mo incident-free) fires; OR a live trial shows the Step-4 baseline conflicts with Stryker runs |
| 66 | AI Factory (`lee-to/ai-factory`) — single-feature workflow pipeline: `/aif-plan` (decompose to tasks) + `/aif-implement` (sequential execution with commit checkpoints) + `/aif-verify` (quality gate) + `/aif-evolve` (skill self-improvement via `.ai-factory/patches/`) + 7 other slash-commands; `--parallel` mode = worktree-per-feature dispatch; landing page aif.cutcode.dev | `/meta-orchestrator` skill — planner/implementer/reviewer role vocabulary + worktree-per-feature pattern as design references | 2026-05-23 | 2026-05-23 | REFERENCE | Meta-orchestrator R-phase patch [§1 row 2 + §3 leapfrog table](research-patches/2026-05-23-meta-orchestrator-prior-art.md) (DeepWiki ×3, WebFetch landing page, WebSearch ×3 — 2026-05-23). **T16 problem-class check:** upstream = «execute single-feature plan with commit checkpoints + post-implementation quality check; single project scope»; ours = «meta-level portfolio orchestration across multiple parallel umbrellas with plan-currency verification + cross-umbrella priority resolution». **Match? Partial — vocabulary only.** Match score 14% (2/14 §7 sub-sections PARTIAL or WEAK, 12/14 MISSING). AIF planner/implementer/reviewer naming + worktree-per-feature pattern transfer as design references; the 4 §7.14 named gaps (plan-actuality, cross-umbrella priority, launch-table, stage-gate) all completely absent in AIF. | AIF ships a `/aif-meta-orchestrate` or equivalent multi-umbrella dispatcher; OR `/aif-roadmap` evolves from single-project milestones into multi-umbrella priority scoring |
| 67 | aif-handoff (`lee-to/aif-handoff`) Kanban runtime — server-side autonomous task pipeline (planning → plan-checker → implementing → review) via `pollAndProcess` cron-driven scheduler; Docker + PostgreSQL/SQLite + Node.js + React Kanban UI; REST API + WebSocket events as user interface (no slash commands) | `/meta-orchestrator` slash-command capability — **REJECT** | 2026-05-23 | 2026-05-23 | REJECT | Meta-orchestrator R-phase patch [§1 row 3](research-patches/2026-05-23-meta-orchestrator-prior-art.md) (4 DeepWiki queries + GitHub README WebFetch + WebSearch ×3 — 2026-05-23). **T16 problem-class check:** upstream = «server-side autonomous Kanban runtime — AI agents plan, implement, review tasks via persistent cron pipeline; Docker+DB infrastructure; single-project»; ours = «session-bound slash-command skill verifying multi-umbrella plan + dispatching, zero infrastructure, project-scope installable». **Match? No — structural mismatch on three axes:** (a) `§7.9` (must NOT write code) vs aif-handoff's primary function (writes code); (b) `§7.10` (zero infra) vs Docker+DB requirement; (c) `§7.1` (user-invoked slash) vs cron-driven (no user-facing slash). Match score 22% surface-level only. Distinct from existing aif-handoff rows (#27 HANDOFF_MODE, #28 paused:true, #29 task annotation, #30 P/I/R pipeline, #43 RuntimeAdapter, #46 Subagents/Skills) — those address different problem classes; this row addresses the meta-orchestrator question specifically. | `/meta-orchestrator` scope expands to include persistent autonomous agent runtime (different product); OR aif-handoff ships a CC-native session-bound slash-command wrapper. **Additive note (Sub-wave A, 2026-05-26):** the `autoQueueMode` LOGIC sub-component was re-evaluated separately and proposed as new SSOT row #88 (ADOPT VOCABULARY + REFERENCE); this #67 REJECT verdict for the **full Kanban runtime** remains correct and unchanged. The sub-component evaluation was deferred from the initial #67 sweep due to the T-CR-A muddied-scope trap. |
| 68 | OhMyOpencode (`code-yeongyu/oh-my-openagent`) Prometheus/Metis/Momus/Oracle/Atlas 3-layer orchestration architecture (planning → execution → verification) with blocking gates between layers, boulder.json session-continuity, `.omo/plans/*.md` Parallel-Execution-Waves section format, 11 agents + 52+ hooks; 59.1k★ + 2.6M+ downloads (`ohmyopenagent.com` = same project as `oh-my-openagent`, renamed from `oh-my-opencode`); BEYOND SSOT #61 (rulesInjector channel-selection use case) | `/meta-orchestrator` §7.7+§7.8 architecture vocabulary — blocking-oracle-gate pattern + iterative-reviewer-loop-until-approve + Parallel-Execution-Waves section format + boulder.json continuity = **ADOPT VOCABULARY (sub-pattern)**; full architecture validates problem class is real and solvable = **REFERENCE** | 2026-05-23 | 2026-05-23 | REFERENCE | Meta-orchestrator R-phase patch [§1 row 4 + §3 leapfrog table](research-patches/2026-05-23-meta-orchestrator-prior-art.md) (4 DeepWiki queries + WebFetch ohmyopenagent.com landing + docs/guide/orchestration.md + docs/reference/features.md + WebSearch ×3 — 2026-05-23). **T16 problem-class check:** upstream = «multi-agent orchestration within single OpenCode session, task-level parallelism, in-session QA gates, single project, OpenCode plugin substrate»; ours = «cross-session, multi-umbrella, repo-aware meta-launcher with real GitHub PR state gates, CC `.claude/skills/` slash-command, install.sh-templatable». **Match? Partial at abstract level; diverges on substrate (OpenCode ≠ CC) + session model (single ≠ cross-umbrella) + 3-of-4 §7.14 gaps (cross-umbrella priority, launch-table, real-git-PR gates).** Match score ~30%. Adopted vocabulary: «blocking oracle gate» (→ Phase -1 cold-review naming), «Parallel Execution Waves section» (→ launch-table structure), «boulder.json continuity» (→ state.md output). Distinct from SSOT #61 (rulesInjector ADAPT, channel-selection injection) — this row addresses the meta-orchestration capability of OhMyOpencode separately. | OhMyOpencode ships CC-native distribution (currently OpenCode plugin); OR Prometheus/Atlas 3-layer architecture proves insufficient for `/meta-orchestrator` §7.3 cross-umbrella scope |
| 69 | Bernstein (`sipyourdrink-ltd/bernstein`) — Python CLI multi-agent orchestration with YAML DAG (`depends_on` task graph) + janitor agent (validation gate between tasks) + per-task tool restrictions; deterministic parallel agent execution with audit-grade logging | `/meta-orchestrator` §7.4 launch-table DAG structure + §7.7 per-task stage gate — **REFERENCE** for DAG model and janitor-as-gate pattern | 2026-05-23 | 2026-05-23 | REFERENCE | Meta-orchestrator R-phase patch [§1 row 6 + §3 leapfrog table](research-patches/2026-05-23-meta-orchestrator-prior-art.md) (DeepWiki + WebSearch — Surface 6 BFR §3 sweep, 2026-05-23). **T16 problem-class check:** upstream = «deterministic parallel agent execution on a defined YAML task graph; assumes plan IS the YAML binding; audit-grade logging»; ours = «verify a markdown plan's currency vs live `gh pr list` state; cross-umbrella scope; CC skill». **Match? Partial structurally — DAG `depends_on` transfers as launch-table model; janitor-as-gate transfers as Phase -1 review pattern.** Match score ~20% on §7.4 + ~25% on §7.7. Not ADOPT — Python CLI runtime; YAML-as-binding assumption breaks our markdown-plan-as-source model; no cross-umbrella scope; no `gh pr list` actuality check. | Bernstein ports to a CC skill or pure-bash distribution; OR `/meta-orchestrator` §7.4 launch-table grows beyond DAG semantics requiring a richer task-graph engine |
| 70 | ComposioHQ/agent-orchestrator — parallel-agent fleet manager with `getMergeability()` PR-readiness signal set (CI status + review decisions + draft state + merge conflicts as composable boolean signals); routes CI failures back to responsible agent | `/meta-orchestrator` §7.7 stage-gate signal vocabulary — **REFERENCE** for the PR-readiness signal set (CI + reviews + draft + conflicts as `is:merged` decomposition inputs) | 2026-05-23 | 2026-05-23 | REFERENCE | Meta-orchestrator R-phase patch [§1 row 6 + §3 leapfrog table](research-patches/2026-05-23-meta-orchestrator-prior-art.md) (DeepWiki + WebSearch — Surface 6 BFR §3 sweep, 2026-05-23). **T16 problem-class check:** upstream = «manage fleets of parallel coding agents; route CI failures back to responsible agent based on intra-PR mergeability signals»; ours = «check whether Stage 1 PRs are merged on GitHub before dispatching Stage 2». **Match? Sub-pattern only — the PR-readiness signal set (CI + reviews + draft + conflicts) transfers as `gh pr list --state merged` decomposition vocabulary.** Match score ~15% on §7.7. Not ADOPT — ComposioHQ is intra-PR routing (single PR, multiple agents), not between-stage sequencing (multiple PRs, one stage at a time). | ComposioHQ extends to between-stage sequencing capability beyond intra-PR routing; OR `/meta-orchestrator` §7.7 signal set diverges from CompositoHQ's `getMergeability()` decomposition |
| 71 | Superpowers `using-superpowers` skill Red Flags table — 13-entry «thoughts that mean STOP—you're rationalizing» tracker disciplining the skill-invocation moment (catches «just a simple question» / «I'll just do this one thing first» / «the skill is overkill» rationalisations before user-facing response) | `/meta-orchestrator §2` cross-umbrella priority-tie resolution problem class «delegation-vs-decision discipline in skill priority-tie resolution» (recommend winner BUT proceed on confirmation; never silently pick mid-session) — **REJECT** | 2026-05-24 | 2026-05-24 | REJECT | Meta-orchestrator round-1 BUILD verdict evidence-trail row (round-2 audit Sub-wave B Invariant 1, commit `d212dae`). **T16 problem-class check:** upstream = «skill-invocation discipline — when a skill might apply (≥1% probability), don't rationalize out of invoking it»; ours = «delegation-vs-decision discipline — when scoring priority across N umbrellas, recommend a winner BUT defer the actual go/no-go to maintainer confirmation; do NOT silently mid-session pick». **Match? No — structural mismatch:** upstream addresses *whether to invoke at all* (binary skill-on/off); ours addresses *who decides among ranked candidates after enumeration* (decision-vs-delegation among multiple ready options). Match score ~5% (shared «don't shortcut to action» surface phrase only — `#pattern-matching-on-name` (T16) trap if conflated). **ADAPT credit:** the actual pattern `/meta-orchestrator §2` instantiates is [`.claude/rules/reviewer-discipline.md §2` Surface-as-decision-needed](../../.claude/rules/reviewer-discipline.md) (4-step protocol: name `DECISION-NEEDED:` → describe both option consequences → flag maintainer escalation → stop without inferring); §2 «recommends winner, proceeds on confirmation» is the launch-table instantiation of that 4-step protocol for cross-umbrella priority scoring. Internal-pattern ADAPT does not get its own SSOT row (SSOT scope = external candidates per §1) — this row is the negative-evidence external-prior-art check that justifies BUILD over ADOPT. | Superpowers ships a separate skill scoping delegation-vs-decision discipline (not skill-invocation Red Flags); OR `/meta-orchestrator §2` scope expands to autonomous-pick-without-confirmation (different verdict re-evaluation under that scope) |
| 72 | OpenHands `issue_duplicate_check_openhands.py` — LLM-driven GitHub-issue duplicate classification agent (`build_prompt` + `normalize_result` into `duplicate` / `overlapping-scope` / `related-but-distinct` / `no-match` buckets); shipped as GitHub Actions workflow | `.claude/skills/meta-orchestrator/helpers/dup-detect.sh` L3 dup-detect — **REFERENCE** for the result-bucket vocabulary (POTENTIAL_DUPE = upstream's overlapping-scope) and for the fast-path-via-explicit-reference pattern; our implementation is deterministic bash (no paid LLM in CI per `no-paid-llm-in-ci.md §1`) | 2026-05-25 | 2026-05-25 | REFERENCE | Meta-orchestrator planner-completeness Stage 1 R-phase patch [§1 row 2 + §5.1](research-patches/2026-05-25-planner-completeness-prior-art.md) (DeepWiki ask_question 2026-05-25 confirmed `issue_duplicate_check_openhands.py` + `build_prompt` + `normalize_result`). **T16 problem-class check:** upstream = «LLM-in-CI semantic duplicate classification of GitHub *issues* (issue-vs-issue)»; ours = «deterministic bash dup-detect between meta-orchestrator *kickoff.md scope* and merged *PRs* (kickoff-vs-PR), session-bound, no paid LLM». **Match? No on substrate (issue vs PR), runtime (CI LLM vs session bash), and constraint (paid vs no-paid-llm-in-ci).** Match score ~15% (vocabulary only). REFERENCE-only: adopted the 4-bucket result vocabulary collapsed into our 2-bucket {POTENTIAL_DUPE, OK} + xref-fast-path. | If `no-paid-llm-in-ci.md §1` policy changes (escape hatch in §4), or if OpenHands ships a non-CI/session-bound variant of `issue_duplicate_check_openhands.py`, revisit ADAPT for the INCONCLUSIVE-zone escalation surface (R-phase §5.1 explicitly defers that to a future AI-agnostic sub-agent). |
| 73 | TaskMaster (`eyaltoledano/claude-task-master`) `analyze-complexity` command — LLM-assisted complexity scoring for PRD tasks; assigns numeric score 1-10 per task via Claude API call; produces expansion threshold recommendations; tightly coupled to PRD-driven planning workflow and paid-LLM invocation per analysis run | L4 decomposition heuristics vocabulary — complexity-tier naming (`fix` / `I-phase-small` / `I-phase-large`) maps to TaskMaster's small/medium/large intuition; **LLM implementation explicitly rejected** per no-paid-llm-in-ci.md §1 | 2026-05-25 | 2026-05-25 | ADOPT VOCABULARY | Planner-completeness R-phase patch [§2 L4 row 1 + §5.2](research-patches/2026-05-25-planner-completeness-prior-art.md) (DeepWiki 3 queries + WebSearch ×3 — 2026-05-25). **T16 problem-class check:** upstream = «LLM-assisted task complexity scoring within a PRD-driven single-project planner; API-billed per invocation; returns 1-10 numeric score for expansion threshold»; ours = «deterministic LOC+surface-count classification for cross-umbrella meta-orchestrator kickoffs; bash-scriptable; zero LLM calls; binary tier assignment». **Match? ~70% vocabulary, ~0% implementation.** Vocabulary match: TaskMaster's conceptual small/medium/large tiers map directly to our fix/I-phase-small/I-phase-large. Implementation mismatch: TaskMaster calls `claude analyze-complexity` (paid API); ours is `wc -l + grep`; both are no-ops if just vocabulary is adopted. Decision: ADOPT VOCABULARY only — the tier naming conventions transfer; the LLM scoring mechanism is incompatible with no-paid-llm-in-ci.md §1 and our deterministic-bash-only constraint. | TaskMaster ships a `--heuristic` flag (no LLM, deterministic rules) for `analyze-complexity`; OR project adds paid-LLM support post-policy-change |
| 74 | Superpowers SDD (`obra/superpowers` `subagent-driven-development`) 3-tier model selection — coordinator applies `small` / `medium` / `large` task classification within a *single plan* to decide which sub-agent tier to dispatch; classification is within-session and within-plan; uses subjective judgment (no LOC/surface metric); tightly coupled to SDD's own coordinator→implementer→reviewer flow | L4 decomposition heuristics vocabulary — SDD's small/medium/large tier vocabulary maps 1:1 to our fix/I-phase-small/I-phase-large naming; SDD SSOT #64 (parent entry: ADOPT for inner-loop dispatch); this row is the vocabulary sub-registration for the 3-tier naming specifically | 2026-05-25 | 2026-05-25 | ADOPT VOCABULARY | Planner-completeness R-phase patch [§2 L4 row 2 + §5.2](research-patches/2026-05-25-planner-completeness-prior-art.md) (verified against shipped `subagent-driven-development/SKILL.md` via DeepWiki 2026-05-25; parent SSOT #64 = ADOPT). **T16 problem-class check:** upstream = «within-plan classification for single-session dispatch; SDD coordinator assigns tier at task-enumeration time; subjective; no LOC/surface metric»; ours = «across-umbrella item classification for meta-session dispatch; deterministic LOC+surface-count rule; kickoff-file input». **Match? 3-tier vocabulary matches, cross-session scope and metric mechanism do not.** Vocabulary adoption: SDD's `small → fix`, `medium → I-phase-small`, `large → I-phase-large` naming is industry-confirmed vocabulary for the three-tier mental model; gives our tier names an externally-validated anchor. Scope mismatch: SDD classifies within a single orchestration plan (inner-loop); we classify across umbrellas for meta-session routing (outer-loop). Metric mismatch: SDD uses coordinator judgment; we use deterministic LOC+surface-count. ADOPT VOCABULARY only — naming transfers, mechanism does not. Cross-ref: SSOT #64 is the parent ADOPT entry for SDD's inner-dispatch loop; this row registers the vocabulary sub-component specifically for the tier-naming surface used in `classify-work.sh`. | SDD ships explicit LOC/surface metric thresholds for its tier assignment; OR SDD extends to cross-umbrella / cross-session classification |
| 75 | Claude Code skill auto-activation by description match (`when_to_use` / `description` field is the routing key; `triggers:` YAML field is ignored — confirmed via memory `reference_skill_when_to_use_field` 2026-05-20) | L5 skill/agent assignment helper precomputes the CC runtime dispatch decision at planning time — parses `.claude/skills/*/SKILL.md` description fields, scores keyword overlap against item descriptions, emits `recommended_skill: <slug>` as a planning-time mirror of what CC's session-runtime would activate via description-match | 2026-05-25 | 2026-05-25 | ADOPT VOCABULARY | R-phase prior-art survey patch §3 row 5 (DeepWiki + WebSearch 2026-05-25, see `docs/meta-factory/research-patches/2026-05-25-planner-completeness-prior-art.md`). **T16 problem-class check:** upstream = CC runtime skill activation by description-match; ours = planning-time L5 recommendation that mirrors what CC would activate at runtime. Vocabulary match: 100% (description field IS our lookup key). Timing gap: planning-time vs runtime — sequencing difference, not problem-class mismatch. No runtime coupling — the helper reads SKILL.md description verbatim, no algorithmic dependency on CC internals. | If CC ships native planning-mode that queries skill descriptions before session start (would supersede our planning-time precompute), or if CC switches the routing key away from `description:` (e.g. activates `triggers:` again). |
| 76 | Superpowers `using-superpowers` meta-skill CSO («Claude Search Optimisation») — semantic skill dispatch via description fields with the «1% rule» enforcement (if ≥1% chance a skill applies, invoke it; description format «Use when …» convention) | L5 helper adopts the CSO vocabulary for routing decisions: `recommended_skill: <slug>` is the planning-time emission of what CSO would propose at runtime. The «Use when …» description prefix convention is reused as the matching surface; no runtime coupling. Pairs with the CC `when_to_use` vocabulary row (same parent verdict). | 2026-05-25 | 2026-05-25 | ADOPT VOCABULARY | R-phase prior-art survey patch §3 row 2 + §5.3 (DeepWiki 2026-05-25, see `docs/meta-factory/research-patches/2026-05-25-planner-completeness-prior-art.md`). **T16 problem-class check:** upstream = in-session semantic skill dispatch by description-match (live AI session decides invocation); ours = planning-time auto-proposal for future-session dispatch (precompute the same decision before session starts). Vocabulary: ~80% match (description-driven routing). Automation moment: planning-time vs execution-time — sequencing gap, not mismatch. ADAPT credits surfaced in cross-layer composition (R-phase §4 item 5) — L5 is a deterministic precompute that does not replace CSO at runtime. | If Superpowers ships a pre-session skill-proposal API (batch planning mode that emits CSO recommendations without invoking the skill); or if upstream changes description-field conventions in a backwards-incompatible way. |
| 77 | Cline Memory Bank committed-markdown sub-pattern (`cline/cline` — `memory-bank/progress.md`, `activeContext.md`, committed markdown in project repo, updated after each session by explicit command or AI trigger) | `/meta-orchestrator` plan-memory Direction B — ADAPT the committed-markdown-in-repo storage pattern (separate factual cache file committed to project repo); REJECT the on-demand update trigger mechanism (ours is deterministic auto-update on every invocation, not human-command or AI-signal-triggered) | 2026-05-25 | 2026-05-25 | ADAPT | R-phase verdict patch `2026-05-25-plan-memory-rphase.md §4` (DeepWiki `cline/cline` 3 queries 2026-05-25). **T16 problem-class check:** upstream = «project documentation maintained across sessions by committed markdown files, updated on-demand or AI-triggered, human-readable status tracking»; ours = «factual-state cache for cross-umbrella wave-plan tracking, auto-updated deterministically on every invocation, machine-readable schema, separate from strategy layer». **Match on committed-markdown sub-pattern: ~85%** (storage format identical; update mechanism diverges: Cline = on-demand, ours = deterministic-auto). ADAPT: adopt storage pattern, design own update trigger. The ~55% match brainstorm §3.1 cited was for the WHOLE Memory Bank concept; the committed-markdown *sub-pattern specifically* matches at ~85% (the only component we adopt). | If Cline ships auto-update on every session-start (removing the «explicit command» requirement), the problem-class match increases and the update-trigger mechanism may be adoptable verbatim — upgrade to ADOPT. |
| 78 | tickgit (`augmentable/tickgit`) — CLI tool for tracking `// TODO:` comments in git history; shows TODO age, author, associated commit; web-dashboard and GitHub Actions CI integration; output format: grouped-by-file table with commit SHAs | Area A surface (g) — `/meta-orchestrator` discovery sweep TODO scanner for `packages/**/*.ts` (DN-1=(a)) — **KEEP-NARROW** | 2026-05-26 | 2026-05-26 | KEEP-NARROW | R-phase prior-art survey patch `2026-05-26-meta-orchestrator-mode-triage-prior-art.md §1` Area A surface-g row (WebSearch ×3 + context7 2026-05-26). **T16 problem-class check:** upstream = «CLI/CI git-history-aware TODO tracker; runs as standalone command or GH Actions workflow; requires git history traversal per invocation; output = web dashboard or table with commit SHAs, author, age»; ours = «in-session bash scan of `packages/**/*.ts` for `// TODO:` lines, aggregated into `todo-<file>-<line>` discovery entries for meta-orchestrator priority-score.sh; output = synthetic-namespace entries, no git-history dependency». **Match: ~35%.** Surface-g does overlap (scan TypeScript source for TODO comments), but our scope is narrower (one directory, one session, no git history, no CI workflow), our output format differs (synthetic namespace entry vs commit-SHA table), and our consumer differs (priority-score.sh vs web dashboard). KEEP-NARROW: upstream is production-grade for the git-aware TODO-tracking problem class; our surface-g is a simpler in-session subset that does NOT need git history. Upstream complexity (git traversal, web UI, GH Actions) would be overhead for our scope. | If meta-orchestrator TODO scanning evolves to require git-history-aware age/author data (e.g. for staleness scoring) — then ADAPT tickgit's git-traversal approach. |
| 79 | Backlog.md pattern (various open-source projects — e.g. `Expensify/App` `BACKLOG.md`, `taskwarrior-web` `backlog.md`) — single committed markdown file listing all pending work items with priority annotations; maintained by human or bot PR; structured by section (High/Medium/Low priority or domain buckets) | Area A survey — open-file discovery alternative to `priority-score.sh` synthetic-namespace approach — **REFERENCE** for the committed-backlog-in-repo pattern; our approach inverts the model (priority-score.sh derives backlog from code/git state rather than maintaining a hand-curated file) | 2026-05-26 | 2026-05-26 | REFERENCE | R-phase prior-art survey patch `2026-05-26-meta-orchestrator-mode-triage-prior-art.md §1` Area A external row (WebSearch «open questions registry markdown» + «backlog tracking markdown file» 2026-05-26). **T16 problem-class check:** upstream = «committed markdown backlog, human-authored or bot-maintained, single source of truth for pending work, manually prioritised»; ours = «dynamic discovery via bash scanning of wave-sequencing-plan.md §0 + orchestrator-prompts + open-questions.md §13.x + TODO comments + residual-patch anchors; machine-derived, no human curation»; **Match: ~10%.** Storage format overlap (markdown) but discovery mechanism entirely different. REFERENCE only: confirms committed-backlog-as-single-SSOT is a validated pattern; our `wave-sequencing-plan.md §0` is the committed-backlog equivalent. Our priority-score.sh extends this by deriving additional dynamic entries, not replacing the committed SSOT. | If the meta-orchestrator migrates to a purely hand-curated committed backlog (removing dynamic discovery) — upgrade to ADOPT and retire priority-score.sh synthetic-namespace surfaces. |
| 80 | aif-handoff delta-tracking extension (hypothetical, not shipped) — `aif-handoff` (SSOT #27/#28 HANDOFF_MODE) currently tracks handoff state but not cross-session delta arrays (`untracked_seen` / `closed_since_last`); closest shipped aif artifact is the handoff-context YAML with `completed_steps` / `next_steps` arrays | Area B §7.1 delta-artefact shape — **DEFER extension of aif-handoff** for delta-tracking; our `_master-backlog-delta.json` schema serves a different problem class (meta-session wave-tracking vs within-session handoff) | 2026-05-26 | 2026-05-26 | DEFER | R-phase prior-art survey patch `2026-05-26-meta-orchestrator-mode-triage-prior-art.md §2` Area B §7.1 row (DeepWiki `obra/superpowers` + WebSearch «aif-handoff delta tracking» 2026-05-26). **T16 problem-class check:** upstream = «within-session handoff state tracking; `completed_steps` + `next_steps` YAML arrays; single session boundary; per-invocation scope»; ours = «cross-session delta tracking; `untracked_seen` + `closed_since_last` JSON arrays; wave-sequencing-plan.md §0 drift accumulates across multiple invocations; meta-orchestrator scope». **Match: ~25%.** Array-of-items structure overlaps; scope, schema keys, persistence model, and consumer entirely differ. DEFER: aif-handoff does not ship our problem class; implementing cross-session delta on top of aif-handoff would require non-trivial fork (different trigger, different consumer, different gitignore model). β-2 sibling JSON sidecar is a BUILD with aif-handoff as REFERENCE-vocabulary for the array schema shape. | If aif-handoff ships a «cross-session accumulated delta» feature with gitignored per-project sidecar support — revisit as ADAPT target. |
| 81 | oh-my-openagent alias routing pattern (`code-yeongyu/oh-my-openagent` — maps user-facing shorthand commands to internal agent dispatch; alias table in SKILL.md or config; routing function computes target based on alias→intent→handler mapping; no external dependency. **Citation corrected 2026-05-27: previously cited `Doriandarko/oh-my-openagent`, which returns GitHub 404 + DeepWiki 404 — canonical owner verified as `code-yeongyu` (59,647 ★, active, last pushed 2026-05-26) via `gh api` in `research-patches/2026-05-26-companion-reuse-omo-deep-dive.md §1.5` owner disambig.**) | Area C triage alias layer — `/meta-orchestrator` DIRECT/SOLO/BUNDLE/PAIR/DECOMPOSE/RESEARCH alias table in SKILL.md body — **REFERENCE** for the alias→dispatch routing pattern; confirms the pattern is production-grade at scale | 2026-05-26 | 2026-05-27 | REFERENCE | R-phase prior-art survey patch `2026-05-26-meta-orchestrator-mode-triage-prior-art.md §3` Area C external row (DeepWiki `code-yeongyu/oh-my-openagent` + WebSearch «AI agent alias routing» 2026-05-26; owner disambig verified `code-yeongyu` canonical, `Doriandarko` 404 — patch `2026-05-26-companion-reuse-omo-deep-dive.md §1.5`). **T16 problem-class check:** upstream = «user-facing shorthand alias → internal agent dispatch; general-purpose command alias system; runtime routing»; ours = «6-alias planning-time vocabulary table → internal Mode A/B/SDD/Queue/R-phase mapping; routing at SKILL.md read-time, no runtime dispatch call». **Match: ~60%** (alias→handler pattern, SKILL.md-embedded table, no external dependency). Our alias set is fixed-6 (DIRECT/SOLO/BUNDLE/PAIR/DECOMPOSE/RESEARCH) mapping to 5 internal modes; oh-my-openagent is extensible and runtime. REFERENCE: confirms alias→dispatch as production-grade pattern; our BUILD instantiation adds the mode-mapping column and override flags (`--force-mode`, `--skip-classify`) not present upstream. | If oh-my-openagent ships a static planning-time alias vocabulary spec format (non-runtime); or if our alias set grows to ≥15 entries (general-purpose at that point, revisit ADOPT). |
| 82 | Superpowers `obra/superpowers` `skills/writing-skills/anthropic-best-practices.md` «Research synthesis workflow» — Steps: Read sources → Identify themes → Cross-reference claims (verify assertions appear in source material; note supporting sources) → Create structured summary (main claim / supporting evidence / conflicting viewpoints) → Final validation loop: «Check that every claim references the correct source document. If citations are incomplete, return to Step 3.» | Orchestrator skill Phase 4.5 self-audit (Cross-reference claims + Verify citations before PR creation; closes 12-wrong-narrow-framing pattern from `companion-reuse-deep-dive`) | 2026-05-27 | 2026-05-27 | ADOPT VOCABULARY | WebFetch `https://raw.githubusercontent.com/obra/superpowers/main/skills/writing-skills/anthropic-best-practices.md` (2026-05-27). **T16 problem-class check:** upstream = «research synthesis: read sources, identify themes, cross-reference claims (verify in source material), structured summary, final citation validation loop with return path»; ours = «pre-PR orchestrator self-audit: verify REPORT checkboxes reference actual tool outputs, confirm file:line citations exist and evidence claims, companion invocation audit, narrow-framing check». **Match: ~70%** on steps 3 (cross-reference) and 5 (validation loop) — our core adoption. Steps 1-2 (read sources + identify themes) not adopted (different artifact type: we audit our own session output, not external sources). Steps 3-4 of our Phase 4.5 (companion invocation audit + narrow-framing check) are niche additions not present in upstream — surfaced as §7 ATTN in Stage 4 patch (`2026-05-27-stage-4-orchestrator-skill-trim.md §7 ATTN-1`). ADOPT VOCABULARY: adopt steps 3+5 shape and validation-loop pattern; rename to fit orchestrator context; add niche steps 3-4 clearly marked as own additions, not Superpowers'. | If Superpowers ships an explicit «agent output verification» workflow (vs. document synthesis) — re-evaluate as ADOPT verbatim. |
| 83 | OhMyOpencode (`code-yeongyu/oh-my-openagent`) Atlas (verification agent) + Prometheus (planning agent) — Atlas: post-execution verification loops, test-run verification, correctness checks; Prometheus: task planning with parallel dependency analysis, execution sequencing; companion agents in the OhMyOpencode multi-agent system | Orchestrator vocabulary alignment §2 — «Orchestrator dispatch + verification loop» concept maps to OhMyOpencode Atlas/Prometheus (D2 resolved 2026-05-27; Stage 4 adds row to `~/.claude/skills/orchestrator/SKILL.md` §2 vocabulary table) | 2026-05-27 | 2026-05-27 | ADOPT VOCABULARY | Stage 4 I-phase patch `2026-05-27-stage-4-orchestrator-skill-trim.md §4` (D2 resolved). Existing SSOT #81 covers oh-my-openagent alias routing (REFERENCE for Area C triage); this row covers Atlas/Prometheus specifically as vocabulary companions for orchestrator dispatch+verification loop — a distinct capability area (planning + verification vs. alias dispatch). **T16 problem-class check:** upstream = «Atlas: post-execution verification agent; Prometheus: planning agent decomposing parallel tasks with dependency graph, coordinating parallel execution»; ours = «Worker (execution subagent) + Reviewer (verification subagent) + Orchestrator (dispatch + coordination) role hierarchy in Mode A/B». **Match: ~80% vocabulary** (Prometheus ≈ our Phase 2 plan decomposition + Phase 3 Mode A/B dispatch decision; Atlas ≈ our Reviewer subagent + Phase 4.5 verification self-audit). ADOPT VOCABULARY: no runtime dep; vocabulary bridge for cross-project readers; companion-free substrate maintained per DECISION=C. Stage 1 matrix §1.3 SSOT basis; prior-art-evaluations.md #81 for alias-routing facet. | If OhMyOpencode ships a compatible SKILL.md or hook format adoptable verbatim as orchestrator inner loop — upgrade to ADOPT. |
| 84 | `claude plugin install <plugin>@<marketplace> --scope user` CLI subcommand — Claude Code's first-class shell-runnable plugin install mechanism; administrative file-management operation that downloads and registers plugin files from marketplace sources (GitHub repos → local cache); verified via `claude plugin --help` + `claude --help` billing-scope check + `code.claude.com/docs/en/costs` + `code.claude.com/docs/en/discover-plugins` probes (2026-05-27) | install.sh K-1 companion-install mechanism — Superpowers + TaskMaster CC plugin install in `install.sh` K-1 section (Stage 5 I-phase) | 2026-05-27 | 2026-05-27 | ADOPT | Stage 2 v3 verify patch `2026-05-27-install-sh-k1-extension.md §4.8` (VERIFIED-FREE). **T16 problem-class check:** upstream = «Claude Code CLI administrative subcommand: download plugin from CC marketplace registry, register at named scope, idempotent de-dup; no LLM call at install-time»; ours = «bash script invoking shell-runnable command to install optional companion tool during framework install». **Match: YES** — exact mechanism class; `claude plugin install ... --scope user` is confirmed shell-runnable OUTSIDE any CC session (not a slash-command); VERIFIED-FREE (no API billing: `claude --help` scopes `--max-budget-usd` to `--print`/`-p` only; CC costs page does not list plugin install; discover-plugins page describes install as file download/registration). ADOPT: primary K-1 install mechanism for SP + TM. Fallback (claude CLI absent) = warn-and-continue per D6 Option A. **Falsifier:** wrong if Anthropic announces billing extension to administrative `claude` subcommands beyond `--print`/`-p` mode — re-verify by re-running `claude plugin install --help` + `code.claude.com/docs/en/costs`. | If Anthropic announces billing extension to administrative `claude` subcommands beyond `--print`/`-p` mode — reclassify and replace active-install with passive pre-seed or print-only. |
| 85 | `pre-commit/pre-commit` pre-push stdin base-ref handling — parses git's pre-push stdin (`<local_ref> <local_sha> <remote_ref> <remote_sha>`) and uses `remote_sha` as the diff base (`remote_sha..local_sha`); for a new branch (`remote_sha` == all-zeros `Z40`) computes ancestors-not-on-remote via `rev-list --remotes`, falling back to all-files at a root push; exposes `PRE_COMMIT_FROM_REF`/`PRE_COMMIT_TO_REF`. Sibling: `lefthook` uses `git diff HEAD @{push}` (push-destination signal). Verified via DeepWiki `pre-commit/pre-commit` + WebSearch ×3 (2026-05-29). | pre-push hook base-ref detection — `packages/core/hooks/pre-push.ts` `resolveBase()` + `pre-push.fallback.sh` resolver: read git stdin `remote_sha` instead of guessing a static `origin/staging` default (which silently no-op'd on consumer repos lacking a `staging` branch) | 2026-05-29 | 2026-05-29 | ADAPT | hook-base-ref-detection R-phase patch [`2026-05-29-hook-base-ref-detection.md`](research-patches/2026-05-29-hook-base-ref-detection.md) §Prior-art (DeepWiki + WebSearch ×3). **T16 problem-class check:** upstream problem-class = *incremental push scope* (don't re-lint files already pushed; `remote_sha..local_sha` is "what's new since the remote tip"); ours = *per-commit trailer scope* (every commit in the push range must carry its trailer). **Match: ~60% — the stdin-parsing *primitive* transfers; the *scope semantics* differ.** ADAPT (not verbatim ADOPT): we reuse pre-commit's `remote_sha`/`Z40`/`rev-list --not --remotes` mechanism, keep `PREPUSH_UPSTREAM_REF` as the CI override (precedence env > stdin > default), and never silently skip (visible warning). Dev side = incremental scope; CI keeps full-PR scope via `github.base_ref`. **Falsifier:** wrong if the project later wants the dev side to enforce *full-PR* scope (re-check every commit since the trunk merge-base on every push) — then the primitive becomes `merge-base` against a *detected* trunk, reintroducing the trunk-guess this ADAPT specifically eliminates. The per-commit trailer invariant + CI backstop make incremental scope a strict improvement regardless (R-phase §Recommended-design). | If the dev-side scope requirement flips to full-PR (maintainer decision deferred per R-phase falsifier), revisit the trunk-detection candidate chain (`@{push}` → refreshed `origin/HEAD` → first-existing `{main,master,develop,staging}`) as the base resolver. |
| 86 | `superset-sh/superset` (Superset) — desktop GUI + CLI + MCP for orchestrating swarms of CLI coding agents across isolated git worktrees, with `.superset/config.json` setup/teardown scripts | Worktree-dispatch + per-project env-setup substrate for parallel CLI agents | 2026-05-29 | 2026-06-01 | REJECT | **Faceted verdict:** **REJECT** (as project substrate) · **KEEP-NARROW** (our enforcement-substrate scope is narrower/orthogonal) · **REFERENCE** the `.superset/config.json` layered-override schema. T16 PARTIAL — overlaps only the non-load-bearing worktree-env-setup slice, **already covered natively** by CC `claude -w` (v2.1.50+, SSOT-adjacent #20) + `WorktreeCreate` hook (PR #279/#282/#284) with better delivery reliability ([rule-enforcement-channel-selection §4](../../.claude/rules/rule-enforcement-channel-selection.md)). Adopting adds a **non-OSI (ELv2, source-available)** dependency — imposing a license-vetting obligation on open-source-only/regulated consumers (not the managed-service clause, which self-install does not trigger) — plus **Bun + Electron + macOS-primary** coupling to an AI-/OS-/license-agnostic files+hooks substrate that ships to consumers. No-paid-LLM **clean** (interactive terminal launch, operator's own keys — DeepWiki P2/P4). v1.2.0, canary/12 h, Linux AppImage tested / Windows untested. Maintainer **may** use Superset as a personal terminal (does not moot Bug 1). **Two-axis clarification (2026-06-01, DeepWiki-verified):** the REJECT is scoped to the *shipped-to-consumer substrate* axis ONLY. On the *operator-workflow* axis — the maintainer now runs Claude Code through Superset — its operator-side features (MCP `start_agent_session` + task/workspace CRUD, multi-device `deviceId`-routed dispatch (→ #99), session-persistence daemon, attention-notifications, Slack-agent (→ #98)) ARE available and MAY enter the `/meta-orchestrator` workflow **as operator-side enhancements with graceful degradation for non-Superset consumers** ([dual-implementation-discipline.md §3](../../.claude/rules/dual-implementation-discipline.md) — same operator-present/consumer-absent pattern as runtime-bridge + aif-handoff). «REJECT» MUST NOT be read as «operator may not use Superset» — that misread produced a false-narrow REFERENCE verdict in the 2026-06-01 survey (row #10 + the Superset rows), corrected here. | §7 triggers: goal→ship-a-GUI; Superset→OSI-open + headless-portable core; Linux/Windows consumer needs env-setup CC-native can't serve; CC removes `--worktree`/`WorktreeCreate`; project enriches the hook with committed-config layer (→ REFERENCE the schema); **sharpened 2026-06-01 (see [`2026-06-01-companion-capability-survey.md §4.1`](research-patches/2026-06-01-companion-capability-survey.md)):** Automations (RRule cron, [`docs.superset.sh/automations`](https://docs.superset.sh/automations)) + MCP `start_agent_session` ([`docs.superset.sh/mcp`](https://docs.superset.sh/mcp)) are a separate surface from the worktree-substrate that #86 originally REJECTED — re-evaluate when multi-device coordination becomes a `/meta-orchestrator` goal (one mac dispatches, another mac runs workers), OR maintainer needs deterministic RRule scheduling surviving laptop close (compare Superset Automations + `superset agents run` against extending `runtime-bridge` with an at-job scheduler). |
| 87 | Portable worktree-creation CLI landscape — Superpowers `using-git-worktrees` (Markdown skill, no CLI bash entry-point), `tfriedel/claude-worktree-hooks` (CC-hook-stdin only, no CLI args), `code-yeongyu/oh-my-openagent` (TS/Bun, in-session), `lee-to/aif-handoff` `gitIsolation.ts` (Node, in-session) | Worktree-create dual-channel — `scripts/create-worktree.sh` portable bash (human/CI/non-CC-agent axis) paired with the CC-native `.claude/hooks/worktree-setup.sh` (`claude -w` axis); both carry the `worktree-create-setup` dual-pair anchor | 2026-05-30 | 2026-05-30 | BUILD | **Faceted: BUILD** (portable script, this axis) · **REFERENCE** Superpowers #65 (AI-session axis). I-phase of research-patch [`2026-05-30-worktree-create-dual-channel.md`](research-patches/2026-05-30-worktree-create-dual-channel.md) §4-§5 (DeepWiki 4 queries + WebFetch + WebSearch ×3, 2026-05-30). **BUILD** the portable script (PR #300, [`scripts/create-worktree.sh`](../../scripts/create-worktree.sh)); **REFERENCE** Superpowers `using-git-worktrees` (SSOT #65) for the orthogonal AI-session axis. **T16 problem-class check:** ours = «portable bash worktree setup callable from CLI / CI / any agent / human; configurable base-ref; node_modules symlinks»; each surveyed upstream = «AI-session prompt-discipline (Superpowers)» OR «CC-hook-stdin-bound (tfriedel)» OR «in-session TS/Node runtime (oh-my-openagent, aif-handoff)». **Match: 0% on the human/CI portable-bash axis** — no candidate exposes a CLI entry-point; the negative-existence claim survived a 4-candidate adversarial survey (BFR §3 layer). First dual-channel pair in the repo (dual-implementation-discipline.md §9 counter → 1). | If any surveyed upstream ships a portable CLI bash entry-point for worktree creation (e.g. Superpowers adds a `bin/create-worktree`), re-evaluate the BUILD half toward ADOPT and collapse the dual pair. |
| 88 | aif-handoff (`lee-to/aif-handoff`) autoQueueMode pool-slot-allocation LOGIC — `processAutoQueueAdvance` sub-component (ascending `position` selection + `scheduledAt` gate + `parallelEnabled`/`COORDINATOR_MAX_CONCURRENT_TASKS` cap + dirty-worktree guard + CAS claim; tag schema: `roadmapAlias`, `rm:<alias>`, `phase:<N>`, `phase:<name>`, `seq:<NN>` auto-assigned at import via `compareRoadmapImportOrder`; evaluated SEPARATELY from full Kanban runtime (SSOT #67 REJECT, preserved as-is) | `/meta-orchestrator` umbrella-sequencing vocabulary + future bash dispatcher priority-ordering design precedent | 2026-05-26 | 2026-05-26 | ADOPT VOCABULARY | **Faceted: ADOPT VOCABULARY** (tag schema: roadmapAlias/phase:N/seq:NN as YAML frontmatter vocabulary for kickoff.md priority encoding) **+ REFERENCE** (pool-slot-allocation algorithm as design precedent for bash umbrella dispatcher). Sub-wave A R-phase patch (2026-05-26-companion-reuse-aif-handoff-autoqueue.md) — 4 DeepWiki probes with within-one-project disambiguation + WebSearch. T16 problem-class check: upstream = «DB-backed async task advancement within one server-side project»; ours = «file-backed sync umbrella ordering within one git repo». Match ~45% at algorithm+vocabulary level (revised from ~22% full-runtime muddied-scope). ADOPT VOCABULARY holds under BOTH J5 Option A (gitignored state) and Option C (tracked kickoff frontmatter); Option C strengthens ADAPT pathway. Distinct from SSOT #67 (full Kanban runtime, REJECT) — this row is the sub-component evaluation; #67 kept REJECT for infra mismatch on three axes. | (1) J5 verdict lands AND Option C chosen → evaluate ADAPT promotion: bash port of pool-slot-allocation reading `phase:`/`seq:` from tracked kickoff.md frontmatter for automated umbrella dispatcher. (2) `/meta-orchestrator` grows to ≥5 concurrent umbrellas requiring automated priority-ordering without human dispatch decision. (3) aif-handoff ships bash-compatible CLI for autoQueueMode (no Docker needed). |
| 89 | oh-my-openagent session-plan-affinity recency algorithm (`code-yeongyu/oh-my-openagent` — `src/hooks/start-work/session-plan-affinity.ts` + `findRecentSessionPlanPath` in `context-info-builder.ts`; scans session message history in reverse-chronological order; extracts file-path references matching PLAN_PATH_PATTERN; returns first match against available-plans set; enables auto-selection of most-recently-referenced plan without explicit user input) | `/meta-orchestrator` optional auto-umbrella-selection — session-aware auto-selection of active umbrella (kickoff.md path) based on recent session message context; avoids requiring explicit umbrella argument every invocation | 2026-05-26 | 2026-05-26 | ADOPT VOCABULARY | Sub-wave B research patch `2026-05-26-companion-reuse-omo-deep-dive.md §2.1+§3.1+§4.3` (DeepWiki `code-yeongyu/oh-my-openagent` query «within-one-project session-history recency fallback», 2026-05-26). **T16 problem-class check:** upstream = «within-one-project plan auto-selection via reverse-scan of session messages for recently referenced plan paths»; ours = «within-one-project umbrella auto-selection via same mechanism». **Match: ~85%.** Algorithm is directly transferable: replace `PLAN_PATH_PATTERN` with kickoff.md pattern; replace `availablePlans` set with discovered kickoff.md paths. ADOPT VOCABULARY: name the pattern `session-plan-affinity`; BUILD a kickoff-path variant if auto-selection is desired. BUILD gate: «/meta-orchestrator invoked ≥10 times without explicit umbrella arg in session» — recency pattern becomes valuable above that frequency. | If auto-umbrella-selection is implemented (BUILD trigger: ≥10 invocations without explicit arg in single session); OR if kickoff.md tracking is formalized per J5 Option C |
| 90 | Superpowers (`obra/superpowers`) `brainstorming` v5.0+ scope-assessment — «multiple independent subsystems» trigger that gates spec refinement until decomposition into sub-projects (each with its own brainstorm → spec → plan → implementation cycle); `writing-plans` Scope Check backstop; no automated orchestration — user manually re-invokes per sub-project; judgment-based (no LOC/phase-count threshold) | `/meta-orchestrator` §2 vocabulary — «sub-project boundary as decision moment» maps to our «umbrella boundary as decision moment» before Stage N+1 dispatch; confirms «scope gating before planning» as externally-validated pattern; REJECT automated-orchestration analogy (Superpowers has none; our Stage-gate automation is BUILD) | 2026-05-26 | 2026-05-26 | ADOPT VOCABULARY | Sub-wave D DeepWiki sweep (5 probes with explicit within-one-project disambiguation per T-CR-A; all confirmed via `obra/superpowers` wiki 2026-05-26). **T16 problem-class check:** upstream = «within-session scope gating for single feature request → multiple sub-projects; AI judgment; no automation»; ours = «cross-session umbrella-stage sequencing; deterministic LOC/surface classifier; scripted Stage N gate». **Match? Vocabulary ~80%, mechanism ~15%, automation 0%.** Sub-project boundary concept transfers as vocabulary; scripted Stage sequencing is BUILD. No existing Superpowers rows (#55/#64/#65/#71/#74/#76) cover brainstorming scope-assessment — new row warranted. | Superpowers ships automated multi-sub-project orchestration (currently none — only sequential manual); OR brainstorming scope-assessment ships a deterministic heuristic (LOC threshold, phase count) making mechanism match upgrade to ADAPT viable. |
| 91 | Bash/shell mutation-testing tool landscape — `agroce/universalmutator` (regexp/Comby line-rewrite engine, language-agnostic, extensible via custom `.rules` + arbitrary `analyze_mutants` testscript; deterministic, no LLM) · `codeintegrity-ai/mutahunter` (LLM-based, language-agnostic) · Stryker (TS/JS/C#/Scala, no bash) · `mewt` (Solidity/Rust/Go core) · BATS/ShellSpec/bunit (bash *test* runners, not mutators) · `shmutate`/`mut-bash`/`bash-mutate` (do not exist). No dedicated native bash mutator exists. | mutation-discipline-umbrella Stage 2 B — automated mutation testing of `.claude/hooks/*.sh` (53 branches across 9 hooks; `end-of-turn-reminder.sh` = 29 branches × 159 LOC the dominant target); Stryker covers TS only (audit §0) | 2026-05-31 | 2026-05-31 | ADAPT | B.1 prior-art survey patch [`2026-05-31-bash-mutator-prior-art-b1.md`](research-patches/2026-05-31-bash-mutator-prior-art-b1.md) (DeepWiki ×2 + WebSearch ×4 + WebFetch; §1 6-item negative-existence check). Gates Stage 1 audit §A.4 «B = GO; ADOPT/ADAPT not BUILD if B.1 surfaces a mature tool». **T16 problem-class check:** upstream = «regexp line-rewrite mutation of arbitrary source + run a user-supplied test command per mutant + tally kill rate» (production for Solidity/Rust/Go); ours = «mutate bash hook source + run its paired-negative `.test.ts` per mutant + tally kill vs the ≥60% audit threshold». **Match: ~85%** — engine + `analyze_mutants` harness are problem-class-identical; the only gap (no bash rules ship) is closed by a custom `bash.rules` file, which is universalmutator's *designed* extension path (not a fork). ADAPT: ADOPT the engine verbatim + author bash operators (data, not engine code). mutahunter REJECT (LLM → [no-paid-llm-in-ci.md §1](../../.claude/rules/no-paid-llm-in-ci.md)). Negative-existence claim («no dedicated bash mutator») survived an adversarial direct-name search (`shmutate`/`mut-bash`/`bash-mutate`). **Falsifier:** wrong only if maintainer toolchain policy forbids a Python dev-tool entirely → fallback to minimal homegrown BUILD with universalmutator as REFERENCE design (B.1 §B.1.6 DECISION-NEEDED, surfaced to maintainer). | If a dedicated, deterministic, native bash mutation tool ships upstream (re-evaluate toward ADOPT verbatim); or if maintainer rejects a Python CI/dev dependency (flip to minimal BUILD + universalmutator REFERENCE). |
| 92 | Superpowers (`obra/superpowers-skills`) `tracing-knowledge-lineages` skill — slash-command `/Tracing Knowledge Lineages` confirmed in [`obra/superpowers RELEASE-NOTES.md`](https://github.com/obra/superpowers/blob/main/RELEASE-NOTES.md) v2.0+ skill list and [`blog.fsck.com 2025-10-12`](https://blog.fsck.com/2025/10/12/superpowers-20-came-out-yesterday-and-might-already-be-obsolete/) debug-output dump | `/meta-orchestrator` Phase 4.5 self-audit + retro citation-chain — «before claiming a capability is new, trace lineage through SSOT + prior research-patches/» | 2026-06-01 | 2026-06-01 | ADOPT VOCABULARY | Survey [`2026-06-01-companion-capability-survey.md §3.1`](research-patches/2026-06-01-companion-capability-survey.md). **T16 problem-class:** upstream = generic lineage-tracing discipline for any agent-authored claim; ours = SSOT-cite + prior-survey-cite before adding a new capability row. Match ~75% on idea, mechanism transferable as a Phase 4.5 doc edit. Pairs with SSOT #82 (anthropic-best-practices «Cross-reference claims»); not subsumed because lineage is *backward* citation chain, while #82 is *cross-source* verification. **Substrate-pure:** vocabulary only, no Superpowers dependency. | Superpowers plugin is vendored / `tracing-knowledge-lineages` invoked as a Skill from inside meta-orchestrator dispatch (would shift to ADOPT); OR Phase 4.5 self-audit rules add a deterministic lineage-trace step (would shift to ADAPT with a principle-test promotion). |
| 93 | Superpowers (`obra/superpowers-skills`) `skills/problem-solving/when-stuck` problem-solving dispatch skill — confirmed in [`RELEASE-NOTES.md`](https://github.com/obra/superpowers/blob/main/RELEASE-NOTES.md): «when-stuck — Dispatch to right problem-solving technique» | `/meta-orchestrator` Phase 3 worker fork-discipline — pairs with the meta-launch kickoff §4c «do NOT pick. Park it» line and SSOT #88 autoQueueMode pool-slot allocation; worker-side analog of the human-side parked-task collector at [`packages/runtime-bridge/src/cli/questions.ts`](../../packages/runtime-bridge/src/cli/questions.ts) | 2026-06-01 | 2026-06-01 | ADOPT VOCABULARY | Survey [`2026-06-01-companion-capability-survey.md §3.2`](research-patches/2026-06-01-companion-capability-survey.md). **T16 problem-class:** upstream = worker hits hard problem, dispatches across problem-solving techniques (collision-zone / inversion / scale-game / simplification-cascades) before re-attempting; ours = worker hits a fork, parks via `manualReviewRequired` for human resolution. Match ~70% on dispatch-tree shape; our pipeline already enforces the discipline in prose (kickoff §4c), this row names the upstream skill so the convergence is visible. Subsumes (KEEP NARROW) the 4 sibling skills (`collision-zone-thinking` / `inversion-exercise` / `scale-game` / `simplification-cascades`) as techniques the dispatch tree references — preventing `#parallel-evolution-creep` per [`build-first-reuse-default.md §4`](../../.claude/rules/build-first-reuse-default.md). | Superpowers plugin is vendored / `when-stuck` invoked as a Skill from inside the worker dispatch (would shift to ADOPT); OR worker-side fork-detection becomes a deterministic gate (would shift to BUILD with a principle test on the kickoff §4c text). |
| 94 | Superpowers (`obra/superpowers-skills`) [`skills/architecture/preserving-productive-tensions`](https://github.com/obra/superpowers-skills/tree/main/skills/architecture/preserving-productive-tensions) skill — confirmed in [`RELEASE-NOTES.md`](https://github.com/obra/superpowers/blob/main/RELEASE-NOTES.md): «preserving-productive-tensions — Keep multiple valid approaches instead of forcing premature resolution» | [`reviewer-discipline.md §2`](../../.claude/rules/reviewer-discipline.md) («surface forks as DECISION-NEEDED; don't pick») + `/meta-orchestrator` Phase 2 planning (present 2-3 approaches without picking) | 2026-06-01 | 2026-06-01 | ADOPT VOCABULARY | Survey [`2026-06-01-companion-capability-survey.md §3.3`](research-patches/2026-06-01-companion-capability-survey.md). **T16 problem-class:** upstream = architectural decision-making — hold multiple valid approaches in tension instead of premature resolution; ours = reviewer-discipline §2 surfaces forks as DECISION-NEEDED; maintainer decides. Match ~90% on idea + mechanism. Same convergence as #93 / #92 — externally-validated vocabulary for a rule we already enforce in prose. Pairs with #93 (worker-side) — together: worker parks, reviewer surfaces, human picks. **Substrate-pure.** | `reviewer-discipline.md §2` is promoted to a principle test (re-evaluate whether the upstream skill name should be cited verbatim in the principle test description); OR Phase 2 planning emits a structured «tensions held» artefact (would shift to ADAPT with a planning-emission schema). |
| 95 | Superpowers (`obra/superpowers-skills`) `find-skills [PATTERN]` CLI helper — surfaced from [`getting-started-with-skills`](https://explainx.ai/skills/obra/superpowers-skills/getting-started-with-skills) skill body: «Check skills list at session start, or run find-skills [PATTERN] to filter» | Orchestrator skill Phase 0 discovery step 6 (`ls .claude/skills/ .claude/rules/ 2>/dev/null` — `~/.claude/skills/orchestrator/SKILL.md` §«Чек-лист discovery») | 2026-06-01 | 2026-06-01 | ADOPT VOCABULARY | Survey [`2026-06-01-companion-capability-survey.md §3 + §4.2`](research-patches/2026-06-01-companion-capability-survey.md). **T16 problem-class:** upstream = pattern-filtered query over installed skills; ours = `ls` enumeration + manual scan during Phase 0. Match ~85% on idea; mechanism is the CLI binary which is **only available** if Superpowers plugin is installed. Currently SSOT #65 holds Superpowers as a *reference*, not installed — so vocabulary-only for now. **Fast-follow:** conditional on Superpowers install decision. | Superpowers plugin is installed in this repo's `.claude/skills/` or vendored as a dependency → Phase 0 discovery step 6 shifts from `ls` to `find-skills [PATTERN]` invocation (would shift to ADOPT). |
| 96 | Superpowers (`obra/superpowers`) Brainstorm Visual Companion — browser-based mockup server with watch-and-serve `.events` file architecture; confirmed in [`skills/brainstorming/visual-companion.md`](https://github.com/obra/superpowers/blob/main/skills/brainstorming/visual-companion.md) + DeepWiki [`obra/superpowers/6.3-visual-brainstorming-companion`](https://deepwiki.com/obra/superpowers/6.3-visual-brainstorming-companion) | Hypothetical `/meta-orchestrator` Phase 2 UX brainstorming surface (none currently exists) | 2026-06-01 | 2026-06-01 | REJECT | Survey [`2026-06-01-companion-capability-survey.md §4.3`](research-patches/2026-06-01-companion-capability-survey.md). **T16 problem-class:** upstream = browser-rendered mockups + diagrams + wireframes during brainstorming (visual content); ours = markdown kickoff prompts + markdown PRs (text content). **No pipeline surface where browser mockups would help.** T-CCS-A counter held: «cool tool, elegant architecture» ≠ «adopt without a concrete gap». Registered as REJECT row for searchability — prevents re-survey under the same framing in future sessions. | `/meta-orchestrator` grows a Phase 2 UX-design surface (no current roadmap signal); OR `companion-capability-survey-v2` revisits «visual brainstorming for non-UX contexts» with concrete evidence of value. |
| 97 | aif-handoff (`lee-to/aif-handoff`) convergence-aware auto-review loop — `AGENT_AUTO_REVIEW_STRATEGY=closure_first` + `AGENT_MAX_REVIEW_ITERATIONS` iteration cap + convergence-detection (auto-close fires when review finds no blocking findings), in `@aif/agent` `coordinator.ts` / `reviewGate.ts`; surfaced as DECISION-NEEDED #11 in the companion-capability survey, maintainer DECISION=A (register the already-adopted vocabulary) | `/meta-orchestrator` meta-launch kickoff §4c autonomous-dispatch trust-dial — `closure_first` strategy + iteration cap cited verbatim in generated kickoffs as the «park don't guess» tuning knob | 2026-06-01 | 2026-06-01 | ADOPT VOCABULARY | Survey [`2026-06-01-companion-capability-survey.md §2 row #11 + §8.5`](research-patches/2026-06-01-companion-capability-survey.md) (DECISION-NEEDED resolved A by maintainer 2026-06-01). **Already operationally adopted** — the generated kickoff §4c sets `AGENT_AUTO_REVIEW_STRATEGY=closure_first` + `AGENT_MAX_REVIEW_ITERATIONS=1` verbatim; this row registers the vocabulary that was in use but unlogged (the exact stranded-convention shape this register exists to prevent). **T16 problem-class:** upstream = convergence-aware autonomous review loop that hands to a human when not converged within N passes; ours = the same loop cited as the autonomous-aif-dispatch trust-dial (high iters = trust aif more / low = hand off sooner). Match ~85% — we consume aif's own runtime env-vars, we do not vendor it. Pairs with #88 (autoQueueMode pool-slot sub-component) and #93 (`when-stuck` worker-side park discipline) — same trip-wire, different sides. **Substrate-pure:** vocabulary only, zero dependency (env-vars set on aif's runtime when the runtime-bridge dispatches; `package.json` companion-dep grep stays empty). | runtime-bridge `cli/answer.ts` ships (the resolve-loop reviewing aif's decisions) → re-evaluate ADAPT promotion (our own convergence-gate over the bridge); OR autonomous aif dispatch runs ≥3 umbrellas and the convergence semantics need a deterministic principle-test gate on the kickoff §4c env-var block; OR aif-handoff changes the auto-close bar from «no blocking findings» to something stricter |
| 98 | `superset-sh/superset` Slack-agent (`runSlackAgent`) — manage tasks / trigger agent sessions from Slack (bot mention or DM), `create_task`/`update_task`/`list_tasks`/`slack_get_channel_history` tools, posts «Thinking…»→progress→summary back to channel; requires **Pro** plan + account-linking; Slack-only (no Telegram). DeepWiki-verified 2026-06-01 (<https://deepwiki.com/search/does-superset-the-cli-codingag_7fb2a91a-6687-4433-abb8-72caa372f5fe>) | Remote-control + attention-notification surface — same class as our aif-handoff **Telegram** + runtime-bridge question-loop (`questions.ts`) | 2026-06-01 | 2026-06-01 | REFERENCE | **Faceted two-axis (verdict column = shipped axis):** REFERENCE shipped · ADOPT-available operator. **Corrects survey row #10** — the survey ran WebSearch-only (no DeepWiki, patch §1) and hit **Apache Superset** (the BI tool) instead of `superset.sh`, producing a false `INCONCLUSIVE-drop`. The Slack-agent **is real**. **Two-axis (per #86 clarification + [dual-implementation-discipline.md §3](../../.claude/rules/dual-implementation-discipline.md)):** *shipped* axis = REFERENCE (can't hard-depend on Superset Pro + Slack for consumers); *operator* axis = available to the maintainer, but **we already cover this need with aif-handoff Telegram** (free, own keys — memory `project_aif_autonomous_fully_configured`), so no operator action needed unless Slack specifically is wanted. **T16:** their problem = manage Superset's task system from Slack; ours = remote notify + answer parked questions during autonomous runs — partial match, already solved via Telegram. | Maintainer wants Slack specifically (not Telegram) for remote agent control → operator-side ADOPT; OR a consumer requests Slack parity → re-evaluate the shipped axis (still blocked by Pro paywall) |
| 99 | `superset-sh/superset` multi-device dispatch — `list_devices` MCP tool + `start_agent_session{deviceId}` routes an autonomous agent session to a specific online device in the org (free tier). DeepWiki-verified 2026-06-01 (<https://deepwiki.com/search/list-all-major-features-and-ca_65216884-8277-49af-865e-50f39e88dcee>) | `/meta-orchestrator` Phase 3 dispatch — the **only genuinely-new capability** we have no analog for: «one machine dispatches, another machine runs the worker» | 2026-06-01 | 2026-06-01 | DEFER | **Faceted two-axis (verdict column = shipped axis):** DEFER shipped · REFERENCE / ADOPT-when-needed operator. **Per #86 clarification:** *shipped* axis = DEFER (consumers without Superset can't get device-routing). *operator* axis = the maintainer runs CC through Superset, so `deviceId`-routed `start_agent_session` is **available now** — but there is **no current multi-machine need** (single-machine workflow), so building a `/meta-orchestrator` integration today = `#integration-overhead-overestimate` / build-ahead-of-need ([build-first-reuse-default.md §4](../../.claude/rules/build-first-reuse-default.md)). REFERENCE the `deviceId`-routing shape as the design precedent for a future multi-machine `runtime-bridge`. **T16:** their problem = run a chosen agent on a chosen device in an org; ours = currently none → STRONG-but-not-yet-needed. | Multi-machine coordination becomes a real `/meta-orchestrator` goal (one mac dispatches, another executes) → open a scoped R-phase «multi-machine runtime-bridge», REFERENCING Superset `deviceId`-routing; OR autonomous runs outgrow one machine's capacity |
| 100 | Claude Code **native** own-stack cluster — Remote Control (`/en/remote-control`), Channels (`/en/channels`, Telegram/Discord/iMessage push-into-session, v2.1.80+ research preview), Routines (`/en/routines`, Anthropic-cloud scheduled + API + GitHub triggers, `/schedule`), Slack (`/en/slack`), background agents/agent-view (`/en/agent-view`). Dual-channel verified 2026-06-01 (primary `code.claude.com/docs/en/overview` + `/routines` + `/channels`; DeepWiki `anthropics/claude-code`). | Own-stack **criterion-zero** ([build-first-reuse-default.md §1.1](../../.claude/rules/build-first-reuse-default.md)) for the remote-control / scheduled-dispatch / parallel-session-watch class — bounds companion adoption for #85 (amux), #86 (Superset Automations), #98 (Superset Slack), #99 (Superset multi-device) | 2026-06-01 | 2026-06-01 | ADOPT | **Own-stack ADOPT (operator); shipped n/a (CC = harness).** Harvest [`2026-06-01-satellite-feature-harvest.md`](research-patches/2026-06-01-satellite-feature-harvest.md) §2/§4b. Stage-1 own-stack sweep flipped amux/Superset-Automations/Superset-Slack to own-stack-redundant. **T16:** their problem = control/schedule/watch agents remotely; ours = same, served by the harness already run. Routines = subscription-bundled (no-paid-llm clean); Channels session-open-only (does NOT supersede runtime-bridge `questions.ts`). 4 adopt-now: Remote Control, Routines, Channels, agent-view. | A need in this class CC native demonstrably cannot serve (true cross-owned-machine `deviceId` routing per #99); OR Routines exits research preview with materially changed limits/cap; OR CC removes one of these surfaces |
| 101 | Claude Code **native `paths:` frontmatter** on `.claude/rules/*.md` — YAML `paths:` field scopes a rule to glob-matched files (glob + brace-expansion `{ts,tsx}`, multiple patterns); rule loads only when Claude works with matching files. Dual-channel verified 2026-06-01 (primary `code.claude.com/docs/en/memory` «Rules can be scoped to specific files using YAML frontmatter with the `paths` field» + `/en/hooks` `InstructionsLoaded` `load_reason=path_glob_match`). **Already partially dogfooded** in-repo: `phase-research-coverage.md` uses `paths:` today. | The CC-native counterpart of our own [`inject-matching-rule.sh`](../../.claude/hooks/inject-matching-rule.sh) (rule-enforcement-channel-selection §4 path-scoped rule injection, `<!-- globs: -->` markers) | 2026-06-01 | 2026-06-01 | ADAPT | **Agnostic dual-channel (maintainer principle «как с хуком», [dual-implementation-discipline.md §3](../../.claude/rules/dual-implementation-discipline.md)):** on CC use native `paths:` frontmatter (zero custom code); for non-CC consumers (Cursor/Aider/Codex) keep `inject-matching-rule.sh` as the portable fallback; shipped rules carry BOTH `paths:` + `<!-- globs: -->` (same path-scope, two channels, `@dual-pair`). **Refinement:** the repo already runs BOTH mechanisms unreconciled (native `paths:` on phase-research-coverage.md + custom hook globs: elsewhere) — F1 is to document them as the intended dual-pair, not to discover `paths:`. **T16:** upstream = load a path-scoped rule when working matching files; ours = same. **Match: yes.** Divergence: CC loads whole rule on **read**; our hook injects a summary on **Edit/Write** — timing differs, reconcile at I-phase. Census [`2026-06-01-capability-census.md §3 F1`](research-patches/2026-06-01-capability-census.md). | DECISION-NEEDED-1 R-phase: add `paths:` to shipped rules + reconcile read-trigger vs edit-trigger semantics + document the dual-pair; OR CC changes `paths:` matching to fire on edit |
| 102 | Claude Code **Routines API-trigger (`/fire` HTTP endpoint)** — per-routine dedicated endpoint, POST with bearer token starts a cloud session (`curl -X POST …/routines/<id>/fire -H "Authorization: Bearer …"`, `experimental-cc-routine-2026-04-01` beta). Dual-channel verified 2026-06-01 (primary `code.claude.com/docs/en/routines` + use-case «Alert triage … calls the routine's API endpoint»). | Own-stack HTTP dispatch substrate — criterion-zero vs aif `API_RUNTIME_*` one-shot dispatch AND our runtime-bridge dispatch (currently broken, memory `runtime_bridge_mcp_dispatch_fix`) | 2026-06-01 | 2026-06-01 | REFERENCE | **Faceted two-axis:** REFERENCE shipped · ADOPT-when-needed operator. The maintainer has Routines on subscription; `/fire` is a working HTTP-dispatch the runtime-bridge rebuild can REFERENCE instead of finishing the broken bespoke REST. Shipped-axis DEFER (consumers without claude.ai subscription can't depend on `/fire`; core stays agnostic). **Caveat:** research preview + daily cap + beta header may break. Census [`2026-06-01-capability-census.md §3 F2`](research-patches/2026-06-01-capability-census.md). | DECISION-NEEDED-2 R-phase: runtime-bridge dispatch rebuild evaluates `/fire` as the operator substrate; OR Routines exits research preview with stable API |
| 103 | Claude Code **config/memory surface** — `claudeMd` settings key (org CLAUDE.md), `claudeMdExcludes` (monorepo ancestor skip), `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD`, `@path` imports (depth 4), symlinked shared rules, `autoMemoryDirectory`/`autoMemoryEnabled`. Dual-channel verified 2026-06-01 (`code.claude.com/docs/en/memory`). | Consumer install-ergonomics surface — how shipped rules/CLAUDE.md are scoped, excluded, shared across consumer projects | 2026-06-01 | 2026-06-01 | REFERENCE | All cheap (config, no dep). REFERENCE for `install.sh` + three-layer authority model design (INSTALL-FOR-AI). Not adopted here (census = research, T5). Census [`2026-06-01-capability-census.md §3 F-bonus`](research-patches/2026-06-01-capability-census.md). | Consumer install design revisits rule-scoping ergonomics; OR a consumer hits a monorepo ancestor-CLAUDE.md collision needing `claudeMdExcludes` |
| 104 | **amux** parallel-`claude`-session multiplexer — two prod repos: `mixpeek/amux` (Python, web-dashboard PWA + REST/SSE + SQLite kanban with atomic task-claiming + self-healing watchdog) and `andyrewlee/amux` (Go TUI, multi-agent, `.amux/workspaces.json` per-workspace setup-commands). Channel-1 (DeepWiki + GitHub README) 2026-06-01. | Parallel-session orchestration / watch / task-distribution — same class as CC background agent-view (#100), our `parallel-subwave-isolation` + `scripts/create-worktree.sh`, aif watchdogs (#45) | 2026-06-01 | 2026-06-01 | REJECT | **Corrects a propagated mis-citation:** «#85 (amux)» in [#328 row #100](research-patches/2026-06-01-satellite-feature-harvest.md), the #295 matrix, and census §2.1 is WRONG — `#85 = pre-commit/pre-commit` (verified `prior-art-evaluations.md:153`); amux had NO row until now. **Own-stack-first kills most:** phone-dashboard → CC Remote Control (#100); YOLO auto-approve → CC `--dangerously-skip-permissions`; per-session MCP → `.mcp.json`; git-conflict-detect → CC worktree-isolation (strictly stronger). **REFERENCE-residue (no CC/own equivalent):** atomic task-claiming (`POST /board/:id/claim`, zero-race distribution — real gap at scale vs our manual dispatch); `.amux/workspaces.json` setup-commands (gap vs bare `create-worktree.sh`); watchdog confirms aif #45 ADAPT path. Census [`2026-06-01-capability-census.md §10.3`](research-patches/2026-06-01-capability-census.md). | Parallel dispatch outgrows manual task-distribution (atomic-claim becomes load-bearing); OR worktree setup needs declarative post-create hooks |
| 105 | **AI Factory** (`lee-to/ai-factory`) **Reflex Loop** — `fix`→`.ai-factory/patches/<ts>.md`→`implement` reads accumulated patches→`/evolve` proposes targeted SKILL.md improvements (user-approved). Files-only, zero infra. DeepWiki + README 2026-06-01 (distinct repo from aif-handoff). | Closed cross-session learning loop — our fixes (rules, patches) don't feed forward into implementation; same mistakes recur. No own/CC equivalent (`memory-codification.md` is the prose-intent analogue) | 2026-06-01 | 2026-06-01 | ADOPT | **Operator-axis ADOPT (cheap = file convention, no dep/infra).** ADAPT to `.claude/`: a `patches/` store read pre-task + an evolve step proposing rule/skill improvements from patch history. **T16:** upstream problem = institutional memory across sessions without external DB; ours = same (we lean on memory + rules but no fix→implement feedback). Match: yes. Census [`2026-06-01-capability-census.md §10.4`](research-patches/2026-06-01-capability-census.md). | DECISION-NEEDED: adopt patch-store now OR DEFER until a recurring-mistake instance is cited (build-ahead-of-need guard); learn-mode URL→SKILL.md folds into our `/ai-docs` |
| 106 | **AI Factory** template-variable substitution (`{{skills_dir}}`/`{{config_dir}}`/`{{settings_file}}`) — one SKILL.md installs to 15+ agents (CC/Cursor/Windsurf/Codex/…). DeepWiki + README 2026-06-01. | Empirical precedent for our **agnostic shipped-axis** ([dual-implementation-discipline §3](../../.claude/rules/dual-implementation-discipline.md)) — AI/OS-agnostic core that degrades per-harness | 2026-06-01 | 2026-06-01 | REFERENCE | AIF is the mature upstream proof that agnostic-skill portability ships in production. REFERENCE the variable-substitution shape for our `install.sh` / shipped-skill design; not adopted now (census = research, T5). Census [`2026-06-01-capability-census.md §10.4`](research-patches/2026-06-01-capability-census.md). | Our shipped skills need multi-harness install ergonomics → REFERENCE the substitution engine |
| 107 | Claude Code **`/goal <condition>`** — session-level completion-condition evaluator (wraps a Stop hook; fresh Haiku checks after each turn; loops until met or cleared; survives `--resume`; `-p`-compatible). Dual-channel (worker: `code.claude.com/docs` + DeepWiki) 2026-06-01; **worker-sourced, not orchestrator-re-verified.** | Our autonomous-loop pattern hand-rolls «keep going until CI passes» in session prose; `/goal` is the native primitive — composes with our existing `inject-session-bootstrap.sh` Stop hook (two Stop hooks fire) | 2026-06-01 | 2026-06-01 | ADOPT | Operator-axis ADOPT (cheap = built-in command/habit). Directly serves the orchestrator-skill «автономно/без остановок» trigger class. Census [`2026-06-01-capability-census.md §10.5`](research-patches/2026-06-01-capability-census.md). **Honesty:** worker-claimed, single-session-unverified — confirm Stop-hook composition in our actual `settings.json` before load-bearing use. | Confirmed in our config → adopt for autonomous waves; OR CC changes `/goal` semantics |
| 108 | Claude Code **orchestrator mechanical-gate hooks** — `SubagentStart` (inject digest into juniors at spawn, $0), `SubagentStop`/`TaskCompleted` (exit-2 force-continue / block-completion on missing REPORT sections), `TaskCreated` (gate on worktree-isolation), `PreCompact` (save wave-state before compaction), `ConfigChange` (block unauthorized `settings.json` changes). Worker-sourced dual-channel 2026-06-01. | Mechanizes orchestrator discipline currently done by prose/manual: REPORT-schema validation (↔ phase-research-coverage §1.7), parallel-isolation gate (↔ parallel-subwave-isolation §1), settings.json self-protection (↔ `feedback_settings_json_agent_uncommittable`) — all deterministic, $0, no paid LLM | 2026-06-01 | 2026-06-01 | ADOPT | **DECISION-NEEDED (operator) — touches `.claude/settings.json` (self-protected, maintainer-landed) + new hooks → own I-phase.** High-value: turns three prose disciplines into deterministic gates. **Honesty:** worker-claimed (exit-2 SubagentStop behavior single-channel docs-only) — bench-test before wiring. Census [`2026-06-01-capability-census.md §10.5`](research-patches/2026-06-01-capability-census.md). | I-phase: bench-test exit-2 + additionalContext contracts, then wire SubagentStart digest-injection + ConfigChange self-protection first (lowest risk, highest leverage) |
| 109 | aif-handoff autonomous-pipeline park — no first-class deliberate-park primitive exists; the only question primitive is `AskUserQuestion` (chat-conversation-scoped & synchronous, `api/src/routes/chat.ts:78`); full task status set (`shared/src/types.ts`) has no `parked`/`awaiting` state. Live+source verified 2026-06-01 (design spec §2 F1–F10). | Agent-issuable deliberate-park for the autonomous task pipeline: stop mid-implementation on a hard fork, ask the operator, resume with the answer (`packages/runtime-bridge/src/cli/park.ts`). | 2026-06-01 | 2026-06-01 | BUILD | No upstream park primitive for the autonomous pipeline (F8 full status set lacks parked/awaiting; F9 chat `AskUserQuestion` is synchronous & chat-scoped — different problem class, **T16** pattern-matching-on-name risk avoided). `park.ts` is a thin consumer CLI (dual of shipped `answer.ts` #323): `PUT /tasks/:id { paused:true, blockedReason, plan }`. `paused:true` is the only agent-reachable stop (coordinator candidate query filters `paused=false`, verified `data/dist/index.js:901`) — a GUARD test pins this load-bearing undocumented dependency so an upstream regression fails loudly. Renumbered #100→#109 (this PR) to yield #100 to the earlier-opened PR #328 (CC-native own-stack cluster — Remote Control etc.), which row #104's `(#100)` refs already point at. The original capability commit `4b9e660` trailer cites `#100`; that trailer lives in the already-merged squash `18bc7a6` (base history, not re-checked by the §7 C1 gate). | Upstream aif ships a first-class `parked` status + answer-carrying resume event (spec §3 C, DEFERRED) → revisit to ADOPT upstream and retire `park.ts`; OR multiple consumers need a documented park contract. |
| 110 | Cross-worktree gitignored-SSOT live sync via symlink-to-canonical (`scripts/link-coordination.sh`) — **upstream evidence:** `thesunny/worktree-env-sync` (symlink mode: one canonical file, N worktree symlinks; env-file-centric CLI) + `worktree.linkFiles` proposal (Archon #1281, `symlink` gitignored dirs rather than copy). Copy-at-creation tools (`git-worktreeinclude`, `git-worktree-runner`, `worktrunk`) are **REFERENCE-only** (copy ≠ live sync; T16 problem-class mismatch: upstream = bootstrap a new worktree once; ours = ongoing live SSOT shared across N worktrees). | Share gitignored orchestrator-prompts (kickoffs, state) across all worktrees by symlinking per-file content to `${CLAUDE_COORDINATION_DIR:-$HOME/.claude-coordination/rules-as-tests-aif}`, surviving `git worktree remove`; supersedes J5 one-way rsync copy (stale snapshot). | 2026-06-01 | 2026-06-01 | ADAPT | **ADAPT (not ADOPT):** no surveyed tool symlinks a coordination dir for an AI-orchestration project; they are env-file-centric CLIs — adopting one wholesale drags in unneeded surface (monorepo/template-interpolation). The adaptation is `scripts/link-coordination.sh` implementing the same mechanism for our payload. **T16 problem-class check:** upstream = symlink gitignored env/config from shared location into each worktree; ours = symlink gitignored coordination-SSOT into each worktree. **Match: yes on mechanism; payload differs (env vs. coordination docs) — no new problem.** Granularity = option (i) per-file symlinks (tracked `README.md` and `*/done.md` stay real per-worktree files, never symlinked — prevents git phantom-delete regressions). Copy-at-creation tools noted as REFERENCE only. R-phase: [`docs/meta-factory/research-patches/2026-05-17-cross-worktree-coord-doc-sync.md §4-§5`](research-patches/2026-05-17-cross-worktree-coord-doc-sync.md). | Upstream ships live coordination-dir sync for AI-orchestration (not just env files) → revisit for ADOPT; OR a new worktree management tool ships `linkFiles`-style directory-level live sync. |

---

## 5. Staleness policy

Entries with `Last reviewed` >180 days surface as candidates for re-evaluation during the next phase entry research session (manual review during [§5.5 Step 1.5](EXECUTION-PLAN.md)). The 180-day threshold reflects the typical cadence between major framework releases — Next.js / Fastify / etc. ship breaking changes on roughly that scale, and a SSOT entry whose «last reviewed» predates the release window is presumed stale until re-verified.

**Format:** all dates ISO 8601 (`YYYY-MM-DD`). Older formats (e.g. `2026-05-08T15:30:00Z` ISO 8601 timestamp) are accepted for parsing but the date column is canonical date-only.

**No automated CI gate yet** — staleness detection is currently a manual phase-research activity, not a workflow check. Rationale: the cost of a false positive (flagging a still-current entry as stale and forcing re-verification) exceeds the cost of a false negative (a stale entry persisting one extra phase). Automated staleness gating is deferred to Phase 8.X+ self-diagnostics tooling per [self-diagnostics-design.md §6](self-diagnostics-design.md). When activated, the gate should warn-only at first (per the same false-positive logic) and only escalate to hard fail after observed FP rate <10% on 10+ phase research sessions.

**Re-verification scope:** when Step 1.5 surfaces a stale entry, the phase research must run a fresh context7 query (≥3 phrasings per Hard Constraint #5) and update the entry in the same commit:
- If still applicable: bump `Last reviewed` only.
- If new evidence changes verdict: new commit changing `Verdict` and `Rationale` (audit trail in git history; do not edit history).
- If the candidate has been deprecated/abandoned: change `Verdict` to `REJECT` with the disqualifier in `Rationale`.

---

## 6. Cross-references

- [EXECUTION-PLAN.md §5.5 Step 1.5](EXECUTION-PLAN.md) — mandatory consult gate.
- [EXECUTION-PLAN.md §3.3](EXECUTION-PLAN.md) — Prior art table (high-level, links into this SSOT).
- [aif-comparison.md §9](aif-comparison.md) — reuse matrix (aif-evolve overlap; cross-ref added Phase 8.8 T10).
- [open-questions.md §13.10](open-questions.md) — LLM v2 triggers referenced from Autogrep entry rationale (Phase 8.8 T2).
- [CONTRIBUTING.md](../../CONTRIBUTING.md) — `Prior-art:` commit trailer convention (added Phase 8.8 T7).
- [packages/core/principles/08-prior-art-cited.test.ts](../../packages/core/principles/08-prior-art-cited.test.ts) — Phase 2 principle validating that research files cite entries here (added Phase 8.8 T3).
